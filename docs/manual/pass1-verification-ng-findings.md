# パス1 無課金検証 NGパターン集約

各動画の `renders/<動画名>/review_checklist.md` で見つかったNG項目を横断的に
ここへ集約する。**この時点では修正しない・収集フェーズ**（2026-08-10〜）。
ある程度パターンが溜まってから、まとめて優先度をつけて対応する方針。

## 運用ルール

1. `review_checklist.md`でNGを見つけたら、下の表に1行追記する
2. 原因調査（`manifest.jsonl`/`timeline.jsonl`/`states.jsonl`を見て、構造化データ側が
   正しいか・生成テキスト側の問題かを切り分ける）をした上で「原因分類」を埋める
3. 同じパターンが複数の動画で再発したら「件数」欄が分かるよう備考に追記
4. `docs/manual/pass1-verification-video-list.md`の進捗と対になる記録

## 原因分類の凡例

| 分類 | 意味 |
|---|---|
| LLMハルシネーション | 構造化データ（move_log/type_hint等）は正確なのに、生成テキストがそれと矛盾する内容を作った |
| LLM作話（情報不足） | contextに該当情報が無い（None/空）状態で、LLMが架空の情報ででっちあげた |
| パイプライン検出漏れ・誤検出 | 状態抽出（OCR/トラッカー）自体のバグ。構造化データの時点で誤り |
| Phi-3固有の癖 | ローカルLLM特有の文体破綻・意味不明化等（Bedrockでは起きない可能性） |
| 複合 | 上記の組み合わせ（パイプライン側の既知の仕様上の制約がLLMの作話を誘発した等） |

## NG一覧

| 動画 | 時刻 | イベント種別 | 実況テキスト | NG理由（ユーザー記入） | 原因分類 | 詳細 |
|---|---|---|---|---|---|---|
| 2026-04-13_06-22-25 | 137.2s | move_single | 「イッカネズミはシャドーボールにやられたわぁ…でも、きっと次のターンも大丈夫！」 | イッカネズミではなくイダイトウがシャドーボールをしている | LLMハルシネーション | `move_log`は「T1:イダイトウのシャドーボール」・`type_hint`も「イダイトウのシャドーボールはイッカネズミにこうかなし」と正確。にもかかわらず生成テキストは使い手を取り違え、かつ「こうかなし」と矛盾する「やられた」という表現を作文した。構造化データは無罪 |
| 2026-04-13_06-22-25 | 162.1s | battle_end | 「こうさんが降参したようです！ えらさんの勝利だ♪」 | えらさんってだれ？ | 複合（パイプライン仕様＋LLM作話） | `_detect_battle_result()`（`src/pipeline.py:2521`）は「勝負に勝った/負けた」の文言のみ判定対象で、**降参は仕様として意図的に判定不能（None）**。`battle_context.battle_result`が空のままLLMに渡り、勝者情報ゼロの状態で「試合終了を実況して」と丸投げされた結果、Phi-3が実在しない「えらさん」という名前をでっちあげた |
| 2026-04-14_21-44-43 | 57.3〜80.5s（ターン0） | （試合内容・状態抽出） | `states.jsonl`で「ランクルス」として記録 | ランクルスはいない。正しくはオオニューラ | パイプライン検出漏れ・誤検出 | 試合開始直後の選出画面OCRで名前を誤認識してロスター登録。turn1（87.3s〜）の正式battle_start時に「オオニューラ」へ正しく切り替わり、以降ランクルスは二度と出現しない＝**同一個体の誤認識**。生成テキスト側は誤ったデータをそのまま使っただけでLLMは無罪 |
| 2026-04-14_21-44-43 | 82.3s | move_used | 「ハッサムがガオガエンにこごえるかぜを打ってきたわね！イダイトウの素早さが下がるのはちょっと困るかも…」 | ハッサムとガオガエンにこごえるかぜを打ったようだ　など | LLM作話（情報不足） | `manifest.jsonl`のcontextは`move_log: []`（空）、`timeline.jsonl`にもこの時間帯の技イベントが1件もない。「こごえるかぜ」「ハッサム」「ガオガエン」の組み合わせは構造化データのどこにも存在せず**完全にLLMの創作**。直後82.8s（battle_start）でもほぼ同内容を繰り返し生成しており、履歴（history）の影響で同じ捏造が連鎖した可能性あり。NG#2（えらさんの勝利だ）と同系統だが、今回は技名まで捏造していてより踏み込んでいる |
| 2026-04-14_21-44-43 | 128.3〜167.9s（ターン1） | （試合内容・状態抽出） | `states.jsonl`でブリジュラスの`hp_pct`が終始null | ブリジュラスは倒れた判定はOKだけどHPは0％、支障はないが0%を検出できるべきかも | パイプライン検出漏れ（軽微・実害なし） | 気絶自体はボール数推定経由で正しく検出（`faint`イベント発火）できているが、HPバーの0%表示自体はOCR/ピクセル解析で一度も捕捉できていない。実況への実害はない |
| 2026-06-03_21-35-48 | 168.7s | battle_end | 「相手のミミロップ、ギガインパクトでブリジュラス狙いかな？ クレッフィはイカサマで相手を混乱させるのもありかも♪」 | 試合終了の実況として的外れ。勝敗にも終了にも触れず、直前ターンの技予測トークのような内容になっている | 複合（パイプライン仕様＋LLM作話） | フレーム確認（168.7s・178s）で**「降参が選ばれました」→自分の勝ち（WIN/LOSE画面）**を確認。`_detect_battle_result()`は「勝負に勝った/負けた」のみ判定対象で**降参は今回も検出できず**、`context.battle_result`が空のままLLMに丸投げされた。NG#2（2026-04-13_06-22-25・えらさんの勝利だ）と**同じ根本原因（降参未対応）の2件目の実例**。ただし壊れ方は異なり、今回は架空の固有名詞を捏造せず「技の予測」という文脈違いの内容を生成した＝Phi-3の失敗モードは状況依存で複数パターンありうる。同じ根本原因が2本連続で踏まれたため優先度を上げる材料になりうる |
| 2026-06-03_21-35-48 | 73.0〜128.5s | （試合内容・状態抽出） | `states.jsonl`でエルフーンの`hp_pct`最小観測値=100（117.4sで既にリストから消失＝気絶済みのはずなのに直前の記録は100%のまま） | エルフーンはひんし判定はOKだけどHP0%付近を一度も検出できていない | パイプライン検出漏れ（軽微・実害なし） | NG#5（2026-04-14_21-44-43・ブリジュラス）と同一パターンの別インスタンス。気絶自体（`控え: エルフーン(ひんし)`）は正しく検出できているが、HPバーの0%付近表示はOCR/ピクセル解析で一度も捕捉できていない。実況への実害はない |
| 2026-06-03_21-35-48 | 126.6〜128.5s | （試合内容・状態抽出） | `states.jsonl`でブリジュラスの最大HPが126.6s時点「165」→128.5s時点「185」に変化 | ブリジュラスのHPは 77%/127:165 が正しいが、69%/127:185 と誤認識されている | パイプライン検出漏れ・誤検出 | フレーム確認（126.6s・128.5s）両方とも画面表示は「127/165」で一貫しており、`165`→`185`は**OCRの最大HP桁の誤読**（`6`→`8`の混同）と確定。128.5sのフレームは「〜に効果はいまひとつだ」のエフェクトテキストが重なる瞬間で、視認性が下がっていたタイミングと一致。誤読後の値はその後のcontext（`ブリジュラス HP:127/185`）に伝播し、以降のmanifestエントリ（#5〜#7）にも残り続けた |
| 2026-06-03_21-35-48 | 121.9s | move_single | 「クレッフィにギガインパクトは威力がいまいちかもね♪」 | ギガインパクトを受けたのはブリジュラス（クレッフィではない） | 複合（パイプライン仕様＋LLM作話） | ダブルバトルで自分側がクレッフィ・ブリジュラス両方在場中に相手のギガインパクトが着弾。`move_log`は「T1:ミミロップのギガインパクト」のみで対象ポケモンの情報を持たず、`type_hint`も「クレッフィにいまひとつ」「ブリジュラスにいまひとつ」の両方を並記するだけで対象を一意に特定できない構造。LLMが誤った方（クレッフィ）を選んで実況した。**構造化データに技の対象を記録する仕組みが無いダブルバトル特有の制約**が根本原因（NG#1イッカネズミ/イダイトウ型と同系統だが、今回はシングルの取り違えではなくダブル特有の対象曖昧性が直接原因） |
| 2026-06-03_21-35-48 | 168.7s | battle_end | 「クレッフィはイカサマで相手を混乱させるのもありかも♪」 | イカサマに混乱の効果はない（相手の攻撃力を使うダメージ技） | LLMハルシネーション（新パターン：技の効果に関する事実誤認） | 技名（イカサマ）と使い手（クレッフィ）自体は`move_log`と一致しており取り違えではない。**技の効果そのものを誤って説明した初めての実例**（これまでのNGは使い手/対象/存在しない技名の誤りだったが、今回は実在する技の効果を誤情報で捏造）。同一の168.7s battle_endエントリで既出のbattle_result未検出問題（本表・上の行参照）と合わせ、この1文に2種類のNGが混在している |

## 傾向メモ（気づいたら随時更新）

- 2026-08-10時点、1本目（2026-04-13_06-22-25）で2件発見。いずれもPhi-3が
  「渡された情報が無い/不十分な箇所を、断定口調ででっちあげる」という共通の癖が見える
  （NG#1は事実と矛盾する断定・NG#2は情報空白を埋める架空の固有名詞）。
  件数が増えたら「情報が無い時は断定を避ける」ようなプロンプト側の安全策の要否を検討する
- 2026-08-10、2本目（2026-04-14_21-44-43）で3件発見。
  - **LLM作話パターンが再発かつ悪化**: `move_log`が空の状態で技名（こごえるかぜ）・使い手・
    対象の3点セットを丸ごと捏造。しかも連続する2イベント（move_used→battle_start）で
    ほぼ同内容を繰り返し生成しており、Phi3Clientの会話履歴（history）が捏造内容を
    連鎖させている可能性がある（要注視・件数が増えたら履歴機構の見直しも検討）
  - **新パターン: 試合開始直後のポケモン名誤認識**（今回はランクルス⇔オオニューラ）。
    LLMではなくパイプライン（OCR/分類器）側の問題。正式battle_start以降は自己修復するため
    実害は限定的だが、選出画面OCRの精度に既知の弱さがある可能性
  - **新パターン（軽微）: HP0%の検出漏れ**。気絶判定（ボール数推定）自体は正しいが、
    HPバーの0%表示は捕捉できていない。実害なしなので優先度は低いが件数を見て判断
- 2026-08-11、3本目（2026-06-03_21-35-48）で2件発見。**`scripts/screen_pass1.py`
  （自動一次スクリーニング）を初めて実戦投入し、フラグ通り2件とも実際にNGと確認**
  （ツールの有効性を実証）:
  - **降参未対応バグの2件目の実例**（NG#2「えらさんの勝利だ」と同一根本原因）。
    フレーム確認で「降参が選ばれました」を確認済み。今回は名前の捏造ではなく
    「技の予測トーク」という文脈違いの内容を生成＝**同じ根本原因でもLLMの壊れ方は
    毎回変わりうる**。2本連続で踏まれたため、`_detect_battle_result()`に降参判定を
    追加する対応の優先度が上がった
  - **HP0%検出漏れの3件目の実例**（NG#5ブリジュラスと同一パターン）。軽微・実害なしの
    傾向が継続中
  - 続けてユーザーが`review_checklist.md`を手動で埋めて**追加で3件発見**（自動スクリーニング
    は対象外の観点なので目視でしか拾えなかった）:
    - **HP最大値の桁誤読**（ブリジュラス165→185）。フレーム確認で画面表示は常に165と確定、
      純粋なOCR誤読。技のエフェクトテキストと重なるタイミングで視認性が下がっていた可能性
    - **ダブルバトルでの技対象取り違え**（ギガインパクトの対象はブリジュラスなのにクレッフィと実況）。
      `move_log`/`type_hint`に対象を一意特定する情報が無い構造的制約が直接原因。**ダブルバトル
      特有の新しい切り口**（NG#1イッカネズミ/イダイトウ型はシングルの取り違えだったが、
      今回は構造データ自体が対象を特定できない設計に起因）
    - **技の効果に関する事実誤認（新パターン）**: イカサマ（相手の攻撃力を使うダメージ技）に
      「混乱させる」という存在しない効果を捏造。これまでのLLM作話は使い手・対象・存在しない
      技名の誤りが中心だったが、**実在する技の効果情報を誤って生成したのは初**。今後は
      「技の効果」も検証観点として意識する必要がありそう
  - **示唆**: 自動スクリーニングは既知6パターンしかカバーしないため、目視レビューは
    引き続き必須（今回も目視でしか拾えないNGが3件あった）。ただしフラグ2件は自動検出のおかげで
    見る前から「ここが怪しい」と分かった状態で臨めたため、ゼロから全部読むよりは負荷軽減できている
- 2026-08-12、サンプリング25本全ての自動スクリーニングが完了し集計したところ合計219件
  （想定より遥かに多い）。**⑥HP0%検出漏れ102件＋③絵文字混入83件で全体の84%を占める**。
  219件を1件ずつ収集フェーズで捌くより先に、③絵文字混入はパターンが単純明快（Meiryoに
  絵文字グリフが無いだけの表示バグで実況内容自体に問題はない）かつ件数が突出して多いため、
  **収集フェーズの原則（この時点では修正しない）から例外的に外し、恒久対策を先に実装**する
  とユーザーが判断。`src/pipeline.py`の`_clean_commentary`に絵文字自動除去
  （`[\U0001F300-\U0001FAFF]`除去）を追加（WSL pytest 743件全パス）。詳細は
  `.claude/skills/video-commentary/SKILL.md`の該当行を参照。これにより③パターンは
  以降の検証では新規発生しないはず（既存25本のscreening_report.mdは実装前に生成した
  ものなのでフラグは残ったまま＝再スクリーニングすれば消える見込み）。残る219-83=136件
  （⑥HP0%検出漏れ102件＋①②④⑤の34件）は引き続き収集フェーズ方針のまま次回以降で対応
