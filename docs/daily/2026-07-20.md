# 日報 2026-07-20

## 今日やったこと

### Claude Code設定: パーミッションdenyルールの修正 ✅

`~/.claude/settings.json` のdenyリストで `Write(.env*)` / `Write(**/secrets/**)` が実際にはブロックとして機能していないと判明（Write系ツールのパーミッションチェックは `Write(path)` 形式にマッチしない仕様のため）。`Edit(.env*)` / `Edit(**/secrets/**)` に修正し、JSON構文も確認済み。

### Fable 5 クレジット課金の問い合わせ対応 ✅

GUIで「クレジットを使いますか？」の確認が出るようになった件を調査。公式メールで確認: Fable 5をプラン内（週間上限の一部）で無料利用できる延長プロモーションが2026-07-19 23:59 PTで終了し、07-20から通常のクレジット課金に切り替わったことが原因と判明（バグではなく仕様通り）。ユーザーはSonnet継続利用を選択。

### キャラクター3Dモデル外注: 進行・デザイン検討 ✅

詳細は `docs/design/character-commission-spec.md` ／ `docs/design/character-commission-messages.md`（いずれも`.gitignore`対象・非公開）を参照。要点のみ記録:

- 絵師候補への相談メッセージを送信（本命1件・バックアップ1件）。本命候補から一次回答を受領（3D化・商用利用・クレジット表記等は良好、着手時期は9月）。ただし相談文中の「AI利用について」の開示に対する明示的な回答が抜けていたため、**沈黙を同意とみなさず**フォローアップで再確認を送信（過去に別候補がAI利用NGで発注断念した前例があるための徹底）
- バックアップ候補の追加調査を実施（既存除外リストと重複しない新規候補を数件ピックアップ。調査中、既存除外候補と同一人物と思われる別プラットフォームの出品を発見し再候補化を回避）
- デザイン詳細を多数言語化・決定: 前髪・アクセサリーの方向性メモ、瞳の色（彩度高めの赤紫〜マゼンタ系を希望しつつ最終は絵師提案優先）、肌のトーン（真っ白は避け健康的な色白に決定）、衣装（セーラー服の着崩し＋アクセサリー、リボンタイは大き目シルエット＋花モチーフチャーム、レグウェア・靴はニーソックス＋スニーカー、配色バランスは絵師に一任）、衣装コスト超過時のシンプル化優先順位、参考イメージ収集用の検索キーワード
- 既存VTuber（にじさんじ・椎名唯華氏）との配色被りリスクを検討。公式設定は白髪（アルビノ寄り）でベース髪色が異なるため大きな懸念はなさそうと判断（瞳の色は未確認・最終は実物イラスト比較で確認予定）
- ドラフト内に残っていた取り消し線（打ち消し線）付きの旧案・重複記述を整理・削除（絵師さんへの提示時の混乱防止）

### 実況プロンプトとの整合性チェック ✅（現状確認のみ）

`src/api/server.py` の `_build_vision_prompt` / `_build_script_prompt` を確認。キャラクター名「花圓くれぴ」・性格設定（元気・甘えん坊・ガチ勢のギャップ）は現状未反映で、汎用的な「熱狂的な実況者」トーンのみだと確認。バグではなく、改善ロードマップ④「実況の口調/知識/スラング改善」の未着手項目そのもの。デザイン確定後にまとめて着手する方針を再確認（コード変更は行わず）。

## 次回やること

1. 絵師候補2件（本命・バックアップ）からの回答待ち。本命候補はAI利用可否のフォローアップ回答待ち
2. 回答が揃い次第、正式発注文（穴あき版）にこれまでの決定事項（衣装・配色・アクセサリー等）を反映して完成させる
3. モデラー（1717021）の回答待ち（衣装3D化コスト・入稿資料要件）→衣装の最終確定
4. 動画量産（残り4本・quickstart手順・ネタバレ検査必須）／server.py改修まとめデプロイ／VMCアバター量産、は前回からの持ち越し（[[project_video_first_policy]]参照）
