# 日報 2026-08-03

## 今日やったこと

### 確定ポーズ＋遷移をplay_and_animate_avatar.py本体へ統合✅完了・commit済み

前回（8/2）確定した7ポーズ（`explore_avatar_poses.py`）と、SLERP＋swing-twist分解の
遷移機構を、実況本編で使う`play_and_animate_avatar.py`に統合。

- **POSESの二重管理を回避**: `pose_tuner_gui.py`が既に`import explore_avatar_poses as eap`
  でPOSES辞書を直接参照していると気づき、同じパターンで再利用（POSESのコピペ複製はしない）
- **表情→ポーズのマッピング**（`_EXPRESSION_POSE`）: Joy=`victory_arms_up`・
  Sorrow=`bow_apologetic`・Fun=`head_tilt_curious`（現状発火する全表情をカバー）
- **新規の設計課題: ボーン競合**。常時スウェイ（Spine/Chest）・ランダム仕草（Head）の
  バックグラウンドスレッドとポーズ遷移が同じボーンを取り合う問題に対し、
  `_suspended_bones`という共有setを追加。`play_pose_reaction`がポーズ再生中だけ
  該当ボーンをそこに加え、`run_idle_sway`/`run_idle_gestures`は送信をスキップする方式で解決
- 旧実装（`send_joy_arm_pump`/`_REACTION_NECK_SEQUENCE`/`_ARM_PUMP_*`）は完全に
  置き換わったため削除。未マッピング表情（Angry等）はNeckの標準うなずきにフォールバック
- **実機fb→2件追加修正**:
  1. 加減速カーブをcubic（3t²-2t³）→quintic（6t⁵-15t⁴+10t³・通称smootherstep）に変更。
     速度だけでなく加速度も両端でゼロになり、動き出し/止まり際がより滑らかに。
     `ease_smoothstep`は`explore_avatar_poses.py`が正本なので1箇所の修正で全体に反映
  2. フォールバックの標準うなずきが「瞬間切り替え→静止」の繰り返し（補間なし）で
     ロボットっぽいとfb→`_send_smooth_keyframes`ヘルパーを新設し、キーフレーム間を
     SLERP+ease_smoothstepで補間する多フレームアニメーションに変更
- 検証用に`scripts/test_vmc_pose_reaction.py`を新規作成（`play_and_animate_avatar.py`
  自体の関数をそのまま呼ぶので本番と同じ動きを確認できる）
- テスト+7件・WSL pytest 502件全パス。**Windows実機検証OK→commit完了✅**
  （`acdb669`「アニメーション検証を実行ファイルに統合」）

### MMDから可愛いポーズ/モーションを取り入れる件→実現性確認まで到達・一旦クローズ

ユーザーから「MMDの可愛いポーズを取り入れたい」と相談。①静止ポーズ抜き出し／
②実際のモーションクリップ再生の2択を提示し②を選択。

- **Git公開範囲の方針確認**: VMD統合ロジックも既存の`explore_vmd_motion.py`と同様に
  Git非公開のままにする方針をユーザーに確認済み（`play_and_animate_avatar.py`本体は
  今回変更せず、将来の再生スクリプトは新規の非公開ファイルにする想定）
- **プリメロ工房パック13本の中身を可視化**: `visualize_vmd_stick_figure.py`に
  `--retarget-v3`（全ボーン共役のみ・`explore_vmd_motion.py`の最終結論と同一）を追加し、
  13本全部の棒人間プレビュー（GIF→ユーザー環境で開けずmp4変換→VSCodeでは開けず
  Windowsエクスプローラーから直接開く必要あり、の2段階の配信トラブルを解決）
- **新モーションソース発見**: ユーザー提示の https://3d.nicovideo.jp/works/td14712
  （「ニコニ立体ちゃん」＝アリシア・ソリッドと同じシリーズの別キャラ）に
  **37個の実モーションキャプチャ（ニコファーレ収録・ステップ1〜37）が付属**。
  規約（`http://3d.nicovideo.jp/alicia/rule.html`）を確認: 個人利用なら商用OK・改変OK・
  二次配布/販売OK・クレジット表記義務なし
- `data/vmd/alicia_solid/`に既にダウンロード済みと判明（16本・全37ステップ中の一部）。
  プリメロ工房と同じ文字化け問題（Shift-JISファイル名がU+FFFDで消失）再発→同じ対処
  （`alicia_step01.vmd`等に英数字リネーム）
- **発見: 全身モーキャプデータ（脚を含む23〜26ボーン・約2分・30fps）**で、
  プリメロ工房の上半身のみの短いエモートとは性質が違う。FKボーン（右足/右ひざ等）に
  実際の回転値が入っていることを確認し、可視化スクリプトに脚を追加（`_SKELETON`に
  下半身+両脚+つま先・`render_frame`のbaseline/scaleも調整）
- 「歩きの腕振りが元と違う」というfbには、棒人間が簡易近似（実モデルの正確な
  ボーン位置/軸とは別物）であることが主因の可能性が高いと説明。本当の検証は
  棒人間ではなく実際にVMC+VRMモデルへのストリーミングで見ること、と案内
- **ユーザー決定: モーション/ポーズの検証はここで一旦クローズ**。外部モーション
  ファイル引用の実現性・ライセンス・技術的な道筋は確認できたので、**3Dモデル外注
  完成（9月予定）後に細部（実際にどの動きを採用するか・微調整）を作り込む方針**に変更

### RVC音声変換用ITAコーパス録音、進捗更新

102文→155文（recitation 324文中）まで録音進行。保存先`data/voices/`（gitignore済み）を
`docs/manual/rvc-voice-setup.md`に記録。

### 実況の口調・知識改善（改善ロードマップ④）実装完了✅・EC2デプロイ済み✅

「口調指定・用語集注入・few-shot」の3本柱（口調指定は7/22実装済み）のうち、
残り2本を実装。`src/api/server.py`に3つの共有定数を追加し、vision（リアルタイム実況）・
script（台本パス・ギャップフィラー）両プロンプトに注入:

1. **`_SLANG_GLOSSARY_LINES`（対戦スラング用語集）**: ユーザーとの相談で段階的に確定。
   基本戦術（積み/展開/受け/通す/縛る/ごり押し/詰み/圧をかける/選出）・役割（アタッカー/
   エース/サイクル/削り要員）・ダブル特有の技分類（集中攻撃/範囲攻撃/全体攻撃）・
   試合展開（見せ合い/選出画面/詰めていく）。テラスタル関連はチャンピオンズ未解禁のため
   除外（解禁されたら追加）。「対面」「起点」はシングルバトル寄りの概念のため不採用
2. **`_TONE_EXAMPLES`（口調のfew-shot例文3本）**: ユーザー承認済み。vision側は
   【状況】【実況】形式・script側はJSON出力に合わせて箇条書き形式に変換して注入
3. **`_DOUBLES_TACTICS_LINES`（ダブルバトルの技・特性・戦術知識）**: ユーザー提供の
   https://champions.pokewiki.net/ダブルバトル/基礎知識 から抜粋。技・特性（即実況で
   使える）とチーム構築寄りの戦術知識の2群に分けて提示し、ユーザーが両方採用を決定
- テスト+8件・WSL pytest 508件全パス
- **プロンプト増量への懸念に実測で回答**: vision約2,000→3,036文字（+52%）・script約
  1,240→2,279文字（+84%）だが、画像込み合計トークンは+35〜40%程度に薄まる。
  Haiku 4.5の$1/$5 per MTok単価で試算するとコスト増は1試合+1〜2円程度・許容範囲と回答
- **Bedrock経由のClaudeはファインチューニング不可**（Titan/Llama/Cohere/Mistralのみ対応）
  であることを確認・案内。代替としてAnthropicプロンプトキャッシュ（静的な用語集/戦術
  知識/few-shot部分をcache_control指定すれば入力コストが約1/10に）を提案したが、
  ユーザー判断で一旦保留（`server.py`は現状cache_control未使用のまま）
- **ユーザーがEC2へデプロイ完了✅**（WinSCP転送＋`sudo systemctl restart pokemon-api`）

## 今日はここで区切り（2026-08-03終了）

## 次回やること

1. 実況の口調/知識改善（改善ロードマップ④）: **実機動画での実況品質確認**（残タスク）
2. `play_and_animate_avatar.py`側の呼吸スウェイ/感情別リアクション/ランダム仕草の実機再検証
3. `fist_pump_right`遷移の不自然さ改善（優先度低め）
4. MMDモーション/ポーズの作り込み: **3Dモデル外注完成（9月予定）後に再開**
5. 声質変更（RVC）: ITAコーパス録音継続（155/324文・残り169文）
6. プロンプトキャッシュ（cache_control）でのコスト最適化（保留中・気が向いたら）
7. ローカルLLM移行（中長期）

**定期チェック**: 2026-09-10 Bedrock Haiku EOL／2026年9月頃 3Dモデル外注進捗確認
（ヒタキ様から次回連絡=8月中旬予定）／2026年9月末〜10月頃 チャンピオンズ
レギュレーション確認
