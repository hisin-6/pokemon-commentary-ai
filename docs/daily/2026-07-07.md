# 日報 2026-07-07

## 今日やったこと

### 案A+B+C 実装完了 ✅（時計注入・通信平滑化の秒ベース化・検証系の時間軸修正）

前日合意した改善案を実装した。

- **案A（時計注入）**: `BattlePhaseClassifier.__init__`に`clock`引数を追加し、内部の`time.time()`5箇所を`self._clock()`に置換。`PipelineRunner`に`_now()`メソッド（動画モード=`_video_frame_pos / fps`・ライブ=実時間）を追加して注入。これでデバウンス・脱出弁・セッション区切り等の全時間閾値が、動画検証でも本番ライブでも同じ時間軸で動く。あわせて時刻トラッキングの初期値を`-inf`sentinel化（動画内時間は0付近から始まるため、初期値0.0だと「最後に見てから十分経過」系の判定が冒頭で誤動作する）。終了画面検出時に外から`_last_event_time["turn_start"]`へ`time.time()`を書き込んでいた箇所も`self._now()`に統一（時計原点の混在はデバウンス判定を恒久破壊するため）
- **案B（秒ベース化）**: `_COMM_ENTRY_FRAMES=2`（サンプリングレート依存）→`_COMM_ENTRY_SEC=0.7`の連続検出秒数方式に変更。本番1Hzでは旧実装と同じ入場遅延、30fpsリプレイでは0.1〜0.3秒の微小ブリップを正しく排除
- **案C（検証系）**: `ocr_logger.py`のJSONLに`video_sec`・`fps`フィールドを追加。`replay_phase_events.py`は`pl.time.time`モンキーパッチをやめてclock注入方式に変更し、時間軸を`video_sec`→`(frame-1)/fps`（`--fps`既定30）→`elapsed`（警告付きフォールバック）の優先順に。結果表に時間軸ソース列を追加

### 重大発見 🔎: 昨日までの「検証OK」は間延び軸のアーティファクトだった

正しい時間軸のリプレイ（WSLスタブ実行）で7本全部NGになった。トレースした結果、旧検証の「OK」は約11倍間延びした`elapsed`軸上でデバウンス15秒≒動画内1.4秒相当に縮んで無害化されていたことによる見かけ上の合格で、真の時間軸（=本番ライブと同じ土俵）では以下3つの握りつぶしバグが存在していた:

1. **move_used発火時に`_last_event_time["turn_start"] = now`を押し込む行**: 本物のコマンド画面は通信フェーズ終了の最短0.1秒後に来るため、15秒デバウンスが直後の正当なturn_startを握りつぶす。ターン欠落の主因 → 撤去
2. **`reset_after_processing("faint"/"move_used")`の同様の押し込み**: faint実況処理後の本物のコマンド画面（最短6秒後）を握りつぶす → 撤去。turn_start多重発火は`_allow_turn_start`フラグ＋15秒デバウンスで引き続き防止（7本リプレイで過剰カウント0を確認）
3. **脱出弁の定数が間延び軸チューニングの遺物**: 真の軸ではセッション区切り120秒 > ターン間ギャップ最小18.1秒のため、脱出弁がほぼ一生開かなかった。診断JSONL 7本（30fps全数OCR）からセッション構造を実測し、`_CMD_SESSION_GAP_SEC` 120→**10**（セッション内途切れ最大5.7s < 10 < ターン間ギャップ最小18.1s）・`_TURN_START_ESCAPE_SEC` 45→**15**（turn_start間隔最小19.5s）に再調整

### 検証結果（WSLスタブ・リポジトリ実コード）

`--processing 0`（=`--input`モード相当・処理中に動画が進まない）:

| 動画 | 実T | turn_start | 判定 |
|------|-----|-----------|------|
| 210143 / 071720 / 081225 / 101329 / 140359 | 5 | 5 | **OK** |
| 090544 | 7 | **7** | **OK**（以前は8だった） |
| 110442（4試合） | 12 | 11 | 既知のOCRデータ上限（battle_start=4は正確） |

### 新発見（未対応・方針要検討）: ライブモードの処理ブロック問題

`--processing 12`（ライブ相当・実況処理中の12秒間フレームを見ない）では各試合およそ1ターン欠ける。コマンドセッションは最短1.4秒しか映らないため、処理ブロック中のフレーム喪失は検出ロジック側では救えない。対策候補:
1. 実際の処理ブロック時間を実測する（12秒想定が過大の可能性。音声再生はスレッド化済みで、ブロックするのは実況文生成＋音声合成）
2. イベント処理の非同期化

`--input`検証（動画が止まる）とライブ運用（フレームが流れ続ける）で挙動が構造的に違う点は今後の検証でも要注意。

## Windows実機検証パス → commit → ターン検出クローズ ✅（午後）

- `replay_phase_events.py --processing 0`（Windows venv）: **6/7 OK・WSLスタブと完全一致**（110442=11は既知のOCR上限・battle_start=4正確）
- フルパイプライン`--input`スモーク（`logs/pipeline_20260707_142636.log`・動画`2026-04-12 16-14-39`）: **G5=実T5一致**・脱出弁1回正常発火（48秒で回復）・「ン」登録0件・正常完走。ERRORはVOICEVOX未起動のみ（意図的）
- commit `0d2462d` → **ターン検出タスクをクローズ**
- 副次観察: プレイヤー側にも幽霊「トランセル」が定期OCR経由で登録されスロット0を占有 → 幽霊問題は両サイドで発生すると判明

## 案D実装: 幽霊ポケモン対策 ✅（午後・未commit・実機検証待ち）

`BattleStateTracker._get_or_create` に2段構えを実装（両サイド適用）:

1. **前方一致吸収**: 短い方（3文字以上）が長い方の前方部分文字列なら同一個体とみなす。「リザード」（リザードン→末尾欠け誤読）が後から来たら既存スロットに吸収、幽霊が先に登録済みなら本物の長い名前にリネーム。実在の前方一致ペア（ゴース/ゴースト）は誤吸収リスクがあるが、同一チームに揃う確率よりOCR誤読頻度の方が圧倒的に高いと判断
2. **満杯時eviction**: `MAX_SLOTS=4`満杯で新規登録できない時のみ、場外かつ未気絶で目撃回数最少のスロットを幽霊とみなして削除。場にいる・気絶済みスロットは本物確定なので対象外

検証: WSL単体テスト5シナリオ全PASS（幽霊後着吸収・幽霊先着リネーム・eviction・全本物時の登録拒否・2文字名の吸収除外）。`_find_slot`等の名前検索は部分一致のためリネーム後も動作。

## 案D実機検証1回目 → 主目的達成・副作用1件発見 → 即修正 ✅（夕方）

実機検証（`logs/pipeline_20260707_145643.log`）で吸収（T1: リザード→リザードン）と**T5ロトム登録成功**（前日まで「満杯で登録できません」だった問題の解消）を確認。

ただし副作用を1件発見: 相手の繰り出しメッセージ「ロトムを繰り出した!」が定期OCRの自分側名前蓄積（`accumulate_player_name`）に混入し、そこから発動したevictionが**本物のペリッパー（目撃0回）を道連れ削除**、相手のロトムが自分側ロスターに登録された。evictionが「定期OCRの誤分類」という既存の混入バグの被害を拡大する構図。

**修正**: `_get_or_create`に`allow_evict`引数を追加し、evictionを繰り出しメッセージ確認済みの高信頼経路（`mark_on_field_by_name`・`register_opponent_on_field`）のみに限定。定期OCR由来の低信頼経路は従来通り「満杯なら諦める」。あわせて`accumulate_player_name`の既登録スロットに目撃カウント（`confidence += 1`）を追加し、本物が「目撃0回」のままeviction誤爆候補になるのを防止。単体テスト7シナリオ全PASS。

## 案D実機検証2回目 → 全項目パス → 案Dクローズ ✅（夕方2回目）

再検証（`logs/pipeline_20260707_152110.log`）で全チェックポイント合格:
- ペリッパーが最後まで自分側に残存（前回は道連れ削除されていた）
- ドドゲザン・相手ロトムともメッセージ経路で登録され、幽霊トランセル（目撃0回）・ベトベトン（目撃1回）を正しくeviction
- 自分側ロスターへのロトム混入 0件
- **最終ロスターが両側とも本物の4匹ぴったり・幽霊ゼロ**（自分=イダイトウ/オオニューラ/ペリッパー/ドドゲザン、相手=フシギバナ/リザードン/ガブリアス/ロトム）

## pytest リグレッション確認 → クリーン ✅（夕方3回目・commit `fee6287` / `b5c2adb`）

- 案Dを`fee6287`でcommit → **幽霊ポケモン問題クローズ**
- Windows venvにpytestを導入して`tests/test_pipeline.py`を実行。初回6failのうち1件（`test_detect_move_used_on_communication_to_animation`）は通信平滑化（7/6導入・入場に連続0.7秒必要）にテストが未追従だったもので、clock注入で時間を進める形に修正 → `b5c2adb`でcommit
- 最終結果 **5 failed / 83 passed**。5件は全て既知のHP割当系pre-existing failure（2026-05-06記録済み）で、**今日の変更によるリグレッションはゼロ**
- メモ: 残る5件は`_make_game_state`が`command_cy`ゲート（4/22導入）を渡していない古いテストの可能性が高い。製品バグとは限らないため、直すならテスト側の追従から

## 全6本×書き起こし突き合わせ → 問題1（幽霊の発生源）対策を実装 ✅（夜）

残り5本の動画もフルパイプラインで実行し、書き起こしと突き合わせて問題点を横断集計（ターン検出は6/6本一致・技ログ9割超・気絶ほぼ全検出。詳細はメモリ参照）。最優先の「幽霊ポケモンの発生源」を調査・修正した。

### 発生源の特定（WSLにrapidfuzzを導入して実験）
1. **2〜3文字のOCRノイズ断片がWRatioのpartial一致でscore=90**: ラン→トランセル・オニ→オニスズメ・スピ→スピアー・ゲン→ケンタロス（全て実測）。幽霊「トランセル」の正体はfaint直後フレームの断片「ラン」だった
2. **中国語ポケモン名フォールバック**が漢字UIラベル「能力」を相手ポケモンとして素通し
3. **y座標誤分類の登録順レース**（相手のユキメノコ・ロトムが自分側に先着登録）

### 実装（4段防御）
1. 短断片ゲート（classifier）: 3文字以下はscore95未満を棄却。正当な短名（ロトム・プデラ→プテラ100点）は影響なし
2. `_CJK_UI_WORDS`ブロックリスト: 中国語名フォールバックからUIラベルを除外
3. 新規登録ヒステリシス: 定期OCR経路（`low_trust=True`）の新規名は2サイクル連続目撃まで保留・メッセージ経路は即登録
4. 同一フレームクロスチェック＋相手ロスター前方一致照合で自分側への誤蓄積を防止

検証: classifier実験12ケース＋トラッカー単体テスト17チェック全PASS。Windows実機再検証は明日（16-14-39と06-25-46の2本推奨）。

## 問題1の実機検証パス → commit → 問題3（HP追跡）対策を実装 ✅（深夜）

問題1（幽霊の発生源）は実機2本で検証パス（幽霊0件・eviction発動0回・リザードン生存）→ commit。

続けて問題3（HP追跡）に着手。WSLのcv2で動画の既知HPフレーム（書き起こしでHP確定済みの瞬間）から直接キャリブレーションした結果:

- **HPバーROI座標は全スロット正確**（相手42.1%↔実42%・65.8%↔実65%・自分46.3%↔実45.9%）
- **真犯人は`slot_index`のライフサイクル**: 交代・気絶で場を離れてもクリアされず、再登場時に古い物理位置のまま別ポケモンのバーを読み続ける（「リザードン43%」の正体はガブリアスのバー、「ペリッパー88%」はオオニューラのバー）

修正4点: ①場を離れたらslot_index解放 ②HP割当の曖昧zip禁止（1対1のみ・スワップ根絶）③表示をHPpx無条件優先→鮮度比較に変更（正確な数値HPが新しければ優先）④スロット割当の重複防止。単体テスト10チェック＋幽霊テスト17チェック全PASS。実機検証は明日。

## 問題3の実機検証1回目 → 残課題3つ発見 → 追加修正 ✅（深夜2回目）

実機検証（`pipeline_20260707_202519.log`）でスロットスワップは解消したが、ペリッパー100%固定等が残存。追加調査で3つの根本原因を特定・修正:

1. **`analyze()`がOCRゲート内でサンプル不足**: コマンド画面は静止していてdiff検出が発火せず、定期1.5秒間隔のみ→安定化窓（連続6サンプル）が埋まらない。アナライザー単体をWSLで0.5秒毎に全編シミュレートしたら完璧に動作（ペリッパー45.4%等を正しく確定）＝アナライザー無罪。**毎フレーム実行に移設**
2. **`_last_stable`がスロット（画面位置）紐づき**: 占有ポケモンが交代しても前任者の確定値を返し続け、新ポケモンが継承（ドドゲザンがオオニューラの88%を取得）。`reset_slot()`＋占有者交代検知（`_slot_occupant`/`slot_reset_cb`）で解消。試合開始時の全リセットも追加
3. **早期割当の曖昧ケース**: 相手2匹中1匹しか把握してない時に「最初に読めたスロット」へ誤割当（リザードン43%再発の原因）。両スロットに読み値があり相方不明なら保留するガードを追加

単体テスト16チェック＋幽霊テスト17チェック全PASS。

## 問題3の実機検証2回目 → 自分側ほぼ完璧・相手側の残課題を追加修正 ✅（深夜3回目）

再検証（`pipeline_20260707_210459.log`）で自分側はほぼ書き起こし通りに（ペリッパー45.4%↔実45.9%・オオニューラ87.9%↔実87.7%・ドドゲザン占有者交代リセット→99.6%）。

相手側はHPpxが1件も出ない問題が残存。原因は**イベント発火時のフレームがメッセージ画面等で相手ネームプレートを読めない**こと（構造化ログでT1以外は相手=[]）。`update()`内のcx割当が相手側に走るチャンスが無かった。→ `assign_slots_from_ocr()`を新設し、相手名が安定して読めるコマンド画面の定期OCRでもスロット割当を実行するよう修正。単体テスト16+17全PASS。

## 問題3の実機検証3回目 → 相手側HPpx開通・交代追従の乱れを追加修正 ✅（深夜4回目）

再検証（`pipeline_20260707_212839.log`）で相手側にHPpxが出るようになり、序盤は正確（リザードン67%↔実65%・フシギバナ82%↔実80%）。ただし中盤の交代ラッシュでリザードン再登場後にガブリアスのバー位置（43-44%）へ誤割当が再発。原因は「相手を見る」パネル等で名前が本来と違う位置に表示され、そのcxで割り当てていたこと。`assign_slots_from_ocr()`にcommand_cyゲート（行動選択画面のみ割当）を追加して対処。単体テスト全PASS。

## 問題3の実機検証4回目 → 真因は相手の引っ込めメッセージ未検出と判明・修正 ✅（深夜5回目）

command_cyゲートでは結果が変わらず、精査の結果T1のスロット割当自体は正しかったと判明。真因は**相手の「リザードンを 引っこめた」メッセージが`_SWITCH_OUT_RE`に無く未検出**なこと。リザードンが場に残ったままslot0を保持し続け、ガブリアス（物理slot0）がslot1に押し込まれて**2匹が互いのバーを読む完全反転**が起きていた（ガブリアス67%⇔リザードン43.4%は互いの実値）。

`_SWITCH_OUT_RE`に「〇〇を引っ（こ|込）めた」パターンを追加。3試合分の実メッセージで正規表現を検証（繰り出しへの誤爆なし）。単体テスト全PASS。

## 問題3の実機検証5回目 → 書き起こしとほぼ完全一致 → クローズ ✅（commit `9b0d5a8` / `1a134cf`）

最終検証（`pipeline_20260707_221834.log`）で「リザードン 引っ込め確認」が検出され、占有者交代の連鎖が完璧に流れた。HP比較: リザードン67%↔実65% / ガブリアス43%↔実42%（反転解消）/ 再登場リザードン67% / ロトム100% / ペリッパー45%↔実45.9%。

残ノイズは単発2件のみ（フシギバナ13%誤確定・T5の一時35%表示）。対策候補は「HP増加禁止ルール」（大幅な"回復"は誤読として棄却）で明日以降の小タスクへ。

問題1を`9b0d5a8`「幽霊対策」、問題3を`1a134cf`「HPなど読み取り改善」としてcommit。**書き起こし比較で挙げた高優先度の問題1・2・3を全てクローズ**。

## 明日やること

1. 問題4: 状態異常の漢字形式（「眠ってしまった」）対応＋HPpx単発ノイズ対策（HP増加禁止ルール）
2. ライブモード処理ブロック問題の方針決定（実測 or 非同期化）
3. turn_history→LLM送信（EC2デプロイ込み）/ 音声出力二重化
4. 全6本回し直しで問題1+3修正後の横断再確認
2. 問題4: 状態異常の漢字形式（「眠ってしまった」）対応
3. ライブモード処理ブロック問題の方針決定（実測 or 非同期化）
4. turn_history→LLM送信 / 音声出力二重化 / HP系pre-existingテスト5件の追従修正
