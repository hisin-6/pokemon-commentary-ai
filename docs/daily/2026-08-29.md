# 日報 2026-08-29

## 今日やったこと

新規録画 `2026-08-28_21-52-34` の実況動画制作（パス0〜パス2＋アバター合成＋サムネ・
チャプター生成）を通しで実施。「事実＋意見」実況ルールの強化に加え、パス1実行中の
実機確認で気絶まわりの新規バグを3件・パス1.6の回収タイミングバグを1件発見し、
その場で原因調査・修正・再検証まで完了させた。

### 「事実＋意見」ルールの強化

2026-08-21実装済みのルール（驚き・期待・不安等の感情や短い予想を「一言添えてよい」）を、
ユーザー要望で強化。「添えてよい」→「できるだけ毎回添えること」に変更し、例文を
口語の感嘆・相槌調に差し替え、ワンパターン化防止の注記も追加
（`src/api/server.py` / `src/commentary/kurepi_persona.py`、2ファイル手動同期）。

### 気絶まわりの新規バグ3件（実機`2026-08-28_21-52-34`のパス1実行中に発見・修正）

パス1完了後、生成されたmanifest.jsonlの内容を実際の試合展開と突き合わせて発見。
一発で直らず、3段階の調査・修正を要した。

1. **種族名の取り違え（2:00.9秒）**: ターン1で自分の場の2匹（ガブリアス・リザードン）が
   同時に相手の範囲技を受け、実際に落ちたのはリザードンなのに「ガブリアスが落ちてしまい
   ました」と誤った名前で実況。通常のfaintイベントには「誰が倒れたか」の確定情報が
   一切渡っておらず、LLMがOCRテキストだけから推測して外れていたのが原因。
2. **蒸し返し・見落とし（6:10.3秒）**: 「既報告」を追跡する仕組み（`already_narrated_deaths`）
   がmove_singleの早期HP0%検出でしか埋まらず、通常のfaintイベント自体が確定させた気絶が
   記録から漏れていた。加えて一部だけ既報告のケースでは「これを言うな」という禁止だけを
   渡していて、どれが本当に新しい情報かLLMに教えていなかった（結果: 本当に新しい相手の
   メタグロスの気絶に触れず、既報告のドドゲザンを蒸し返す実況になっていた）。
3. **保留フラッシュ時の未確定情報**: 上記1の修正（faint_focus注入）が最初は全く発火せず
   原因不明化。ログを精査した結果、このゲームは「たおれた」を即座に実況せず次のmove_used
   と統合するため一旦「保留（pending）」する仕組みがあり、**保留を開始した瞬間にはまだ
   気絶がトラッカーに確定していない**（メッセージOCRでの確定は22秒後）ことが判明。
   `_flush_pending_faint`（実際にBedrockへ送る関数）が保留開始時点の古いスナップショットを
   そのまま使い回しており、フラッシュする時点（確定後）で状況を取り直す処理が無かった
   のが真因。フラッシュ直前にトラッカー最新状態でfaint_focusを再計算するよう修正して解決。

いずれも`src/pipeline.py`のみの変更（`server.py`側の文言調整は種族名バグの対策に伴う
微修正のみ）。**修正の都度、同じ動画でパス1を再実行し実機で効果を確認**という
サイクルを3回繰り返した（推測ベースで進めず、都度ログ・manifest.jsonlの生データで
裏取りしたため時間はかかったが、最終的に全て実機確認で解決を確認）。

### パス1.6（予測→回収）の回収タイミングバグ

上記2の修正の副作用として発覚。「決定的な出来事」（回収アンカー）を機械的に
「manifest.jsonl上の最後のfaintイベント」で選んでいたが、この試合は相手が
ムクホークを残したまま降参した決着だったため、真の決着（降参）より前の
faintイベント（しかも上記2の重複スキップ最適化で内容が書き換わっていた）が
誤って選ばれ、回収文が決着の42秒以上前・内容とも無関係なタイミングで再生される
不具合が判明。`find_decisive_event`（`scripts/generate_predictions.py`）に
「降参決着（`battle_surrendered=true`）の場合はbattle_end自体を優先する」ロジックを
追加して解決（通常のKO決着は従来通り最後のfaintを優先・挙動不変）。

### 動画制作は最後まで完走・実機確認済み

修正後、パス0（選出前チームプレビュー）〜パス1〜パス1.5（フィラー）〜パス1.6
（予測→回収）〜ネタバレ検査（exit 0・先読みなし）〜パス2（biim合成）〜アバター合成
（v2c・四国めたん公式VRM）〜サムネイル・チャプター生成まで全工程完走。
**ffmpegでフレーム抽出し、実際の画面と字幕・戦況パネルの内容が一致することを目視で
直接確認済み**（種族名取り違えバグの修正箇所も含む）。動画のYouTube投稿はユーザーが
後日実施予定。

パス2の合成時、スケジュールの空き不足により回収系ペアの片方（選出予想の回収1件・
条件系予測の予測1件）が破棄される事象を確認。実イベント優先の正常動作でありバグでは
ないため、ユーザー判断で容認して本実行した（該当箇所: 回収だけ残り予測が聞こえない
セリフが1件残っている）。

## 今日の変更まとめ（コード）

- `src/pipeline.py`: faint_focusを通常faintイベント・保留フラッシュ時双方に注入する
  ロジック新設／faint_repeat_hintの新情報を明示する positive hint 追加／既報告集合の
  更新をfaintイベント単位に拡張
- `src/api/server.py` / `src/commentary/kurepi_persona.py`: 「事実＋意見」ルール強化
  （頻度up・口語化）／faint_focusプロンプト文言を通常faint対応に修正
- `scripts/generate_predictions.py`: `find_decisive_event`に降参決着判定を追加
- `tests/test_pipeline.py`（`TestFlushPendingFaint`新設等）／`tests/test_render_sink.py`／
  `tests/test_generate_predictions.py`／`tests/test_server.py`: 新規・更新テスト多数
- WSL pytest **1085→1091件**全パス
- **未コミット**（git操作はユーザー実施ルールのため）

## 今日やったこと（続き・午後セッション）

`2026-08-28_21-52-34`のreview_checklist.md作成・NG調査から始まり、新機能追加
（メガシンカ実況）、口調改善、新規動画1本の完全制作、実況NG6件の原因調査・5件の
恒久対策までを1セッションで実施。

### review_checklist.md作成・NG3件調査（`2026-08-28_21-52-34`）

`scripts/generate_review_checklist.py`で自動生成→ユーザーが動画を見ながら記入。
致命的なNGは無しとのことで、以下3件を深掘り調査:

1. **①faintイベント消失（修正済み）**: `_flush_pending_faint`が`faint_focus`は
   フラッシュ時点の最新状態で再計算するのに、`battle_context`（控え欄の「(ひんし)」
   表記）は保留開始時点の古いスナップショットのまま使い回していた直し漏れ
   （8/29午前の修正の続き）。場に出たまま連続気絶したポケモンが控え欄に一度も
   現れず、既報告チェックが「全員報告済み」と誤判定してfaintイベント自体を
   握りつぶしていた。`_refresh_condition_hint`と同じ要領でfield/bench系キーを
   フラッシュ時点に作り直すよう修正。WSL pytest 1091→1092件
2. **②同名ミラー戦のside不明**: 既知の様子見NG（技ログのside判定、実害小）
3. **③faint_focusヒント無視の疑い**: 合成faintの対象指示があるのにLLMが別の
   顕著な情報（新ポケモン登場）に引きずられた疑い。プロンプト遵守の問題で
   確証にはEC2側`DEBUG_PROMPTS=1`が必要なため記録のみ（`pass1-verification-ng-findings.md`
   に追記）

### メガシンカの変身アニメーション区間を実況できるように新機能追加

ユーザー要望「メガシンカ時の演出が無実況になっている」への対応。`move_single`と
同じ設計で新規`event_type="mega_evolution"`を追加。検出時（`_update_mega_evolution`）
に専用実況を1件ディスパッチする`_dispatch_mega_evolution_commentary`を新設。
将来のZワザ/ダイマックス/テラスタルは画面文言未確認のため見送り（同じ設計で
横展開できる形にはしてある）。WSL pytest 1092→1104件。

### neutralペルソナの口調を「ですます」偏重から改善

ユーザーfb「もっと喋り口調に、ですます調を少なく」。`NEUTRAL_CHARACTER_INTRO`/
`NEUTRAL_TONE_EXAMPLES`に「丁寧語だけに偏らずタメ口寄りの言い切りも混ぜる」指示・
お手本文例を追加（キャラの名乗り・自称は引き続きOFF）。WSL pytest 1104→1106件。

### 新規動画`2026-08-28_22-11-26`を完全制作

パス0（選出前チームプレビュー）〜パス1〜パス1.5〜パス1.6（予測→回収）〜ネタバレ検査
（exit 0）〜パス2（biim）〜プレイヤーコメント（v2d・8件）〜アバター合成（v2c）〜
サムネイル・チャプター生成まで全工程完走。**メガシンカ実況の実機初確認**（305.1s
「おっ、相手のムクホークがメガシンカだ！」・演出画面と字幕の一致をフレームで確認）。

制作中に2件の副産物バグを発見・その場で修正:
- **予測回収系プロンプトの口調修正漏れ**: `_build_payoff_prompt`/
  `_build_selection_prediction_prompt`が`_NEUTRAL_CHARACTER_INTRO_LINES`とは別に
  intro_linesをコピペでハードコードしており、上記の口調修正が反映されていなかった。
  同じ指示を追加し、パス1.6を再実行して口調が変わったことを確認（「〜ですね」→
  「〜だね」）。WSL pytest 1106→1108件
- **プレイヤー吹き出しの文字数オーバー**: `render_bubble_overlay.py`は最大3行までしか
  表示できない仕様（フォント32px・幅460px）で、1件（39文字）が3行に収まらず末尾が
  省略された。テキストを28文字に短縮して再合成

### プレイヤーコメント8件から実況NGの原因を調査→6分類・5件を恒久対策

ユーザーが動画を見ながら8件のツッコミを提示（0:01〜6:21）。全件を`bubbles.jsonl`化
する過程で、指摘内容そのものが実況ミスの記録になっていたため、manifest.jsonl・
パイプラインログと突き合わせて原因調査を実施:

| # | 内容 | 原因分類 | 対応 |
|---|---|---|---|
| ① | 「2ターン連続」の誤り | LLMのハルシネーション（技ログ1件しか無いのに数字を創作） | 出力ルールに抑制指示を追加 |
| ② | キラフロルの技名帰属ミス（だいちのちから→まもる） | ヒント文言の「まもる」という単語にLLMが引きずられた | `_voided_move_effect_hint`の注記から技名を排除 |
| ③ | じしん+アイアンヘッドの複合ダメージを単発扱い | move_singleの設計方針の限界 | 見送り（設計変更要） |
| ④ | 交代の蒸し返し（キラフロル→イダイトウを誤った起点で再言及） | 交代の既報告チェックが無かった | `_compute_no_new_switch_hint`新設（faint_repeat_hintと同発想） |
| ⑤ | あまのじゃくの効果誤認 | move_effect_hintが特性を考慮しない設計 | 見送り（特性DB統合要） |
| ⑥ | battle_end/prediction_payoffの内容重複 | 独立生成で重複チェック無し | `_build_payoff_prompt`に「重複させない」指示追加 |

さらにユーザーからの「パス1・パス1.5・パス1.6で内容が重複していないか」という質問を
きっかけに、パス1↔パス1.5（フィラー）の重複も実際に発生していたことを発見
（216.9sの「エルフーン一撃で落とした」→13秒後のフィラーで「エルフーンが落ちた」と
再度同じ事実を反復）。フィラー生成プロンプトの「収録済み実況と重複しない」という
抽象的な指示を、具体例つきで強化（「同じ情報量で言い直さない」「新しい話題に
つなげる」を明示）。途中、自分が追加した文言の「倒れた」という単語が既存のネタバレ
検証テストと衝突する事故があり、「場を離れた」に差し替えて解消。

対策済み①②④⑥＋パス1.5重複強化の実装で、WSL pytest 1108→1123件（+15件）。

### 今日の変更まとめ（コード・午後セッション分）

- `src/pipeline.py`: `_flush_pending_faint`のbattle_context最新化／
  `_dispatch_mega_evolution_commentary`・`BEDROCK_EVENTS`拡張（メガシンカ）／
  `_voided_move_effect_hint`・`_compute_no_new_switch_hint`新設（独立関数化・
  技名混同対策／交代蒸し返し対策）
- `src/api/server.py`: `VALID_EVENT_TYPES`・`_build_vision_prompt`にmega_evolution
  追加／neutralペルソナ口調修正（本体＋payoff/selection_prediction系の同期漏れ対応）／
  `_build_payoff_prompt`の重複対策／`_build_script_prompt`の重複抑制強化／
  回数創作禁止ルール追加
- `src/commentary/kurepi_persona.py` / `src/commentary/phi3_client.py`: 上記の
  手動同期（mega_evolution分岐・口調・no_new_switch_hint・回数創作禁止ルール）
- `renders/2026-08-28_22-11-26/`: 新規動画一式（bubbles.jsonl含む）
- `docs/manual/pass1-verification-ng-findings.md`: 今回の調査結果を追記
- テスト多数追加（`test_pipeline.py`・`test_server.py`・`test_phi3_client.py`）
- WSL pytest **1091→1123件**全パス
- **未コミット**（git操作はユーザー実施ルールのため）

## 次回やること

- 午前・午後セッション分のコード変更のコミット・EC2デプロイ（`server.py`変更を
  複数回含むため一括デプロイでOK・いずれもユーザー作業）
- `2026-08-28_21-52-34`・`2026-08-28_22-11-26`のYouTube投稿（ユーザー実施予定・
  チャプターは各`chapters.txt`を概要欄に貼付）
- 次回の新規録画で今日実装した5件の修正（①②④⑥＋パス1.5重複強化）とメガシンカ
  実況・口調改善の実機効果を横展開の中で確認
- 未対応の③（複合ダメージの複数技合算）・⑤（特性を考慮したmove_effect_hint補正）は
  設計変更が必要なため引き続き見送り
- パス2でのスケジュール空き不足によるペア破棄（選出予想回収・条件系予測1件ずつ）は
  今回は容認したが、頻発するようなら`_FILLER_MAX_SHIFT_SEC`の調整や予測ポイントの
  優先度付けを検討
