# 日報 2026-08-23

## 今日やったこと

新規録画（`D:\ゲーム録画\2026-08-23 22-15-43.mp4`）から実況動画を1本、パス1〜v2c
アバター合成・チャプター・サムネイルまで通しで制作。コード変更は無し（動画制作のみ）。
`renders/2026-08-23_22-15-43/` 一式完成。

### 制作フロー

1. **パス1**（ユーザーがWindows側で実行）: `--persona neutral`。manifest 23件・wav 23件で
   数が一致・neutral口調（♪等の混入なし）を確認
2. **パス1.5**（ユーザー実行）: フィラー6件生成
3. **パス1.6**（ユーザーがdry-run実行）: 「予測ポイント（場のコンディションを確立した
   move_single）なし」で0件・正常終了。本実行は不要と判断しスキップ
4. **ネタバレ検査**: `check_spoilers.py` exit 0（技名先読みなし）。タイムライン目視で
   先読み系の問題は無かったが、非先読み系の気になる点を2件発見（下記「見つかったNG」参照）
5. **パス2**（biim合成）: 1回目の合成完了後、ユーザーが動画を視聴して7件のNGフィードバック
   （タイムスタンプ付き）→ `bubbles.jsonl` に整形して書き起こし → パス2を再実行し
   プレイヤー吹き出し（v2d）を反映。フレーム抽出で表示を目視確認（字幕と非干渉・
   「まだ登場していない」を指摘する吹き出しが該当フレームの状況と一致することも確認）
6. **v2cアバター合成**: ユーザーがVMC＋四国めたん公式VRMで録画
   （`2026-08-23 23-09-17.mp4`）→ 既存実績値
   （`--avatar-crop "663:765:631:314" --avatar-width 700 --avatar-x-shift 80
   --avatar-y-shift 250`）をそのまま流用して合成。フレーム確認でクロマキー良好・
   戦況パネルは自分の場2匹目の技名のみ隠れる程度（既知の許容トレードオフ通り）
7. **チャプター生成**: `chapters.txt`（オープニング〜ターン5、7チャプター）
8. **サムネイル生成**: `--avatar-video`のみ指定の標準運用でOK。faint（t=124.2s）を
   自動選択

### 見つかったNG（ネタバレ検査時・非先読み系）

`check_spoilers.py`は通過（先読みなし）だが、タイムライン目視で以下2件を発見。
先読みではないため合成はブロックせず進行、プレイヤー吹き出し検討用にメモ:

- **254.1s**: 既に187.2sで気絶済みの「オオニューラ」を、faintイベントが再度
  「相手のオオニューラが落ちました！」と実況（二重報告）。原因未調査
- **259.0s/265.9s**: 「ペリッパーが落ちた！ガブリアスのいわなだれが〜」をほぼ同文で
  2回連続実況（move_singleとfaintの別イベントが同一事象を語っている）。259.0s側は
  パス2の遅延超過破棄ロジックで自動的に落ちたため、最終動画には265.9s側のみ残った

### ユーザーfb（7件・プレイヤー吹き出しとして反映済み）

`renders/2026-08-23_22-15-43/bubbles.jsonl` に反映（時刻は動画内時刻）:

| 時刻 | 指摘内容 |
|------|---------|
| 0:52 | 「統一」という言葉の誤用 |
| 1:38 | ガオガエンのひるみに言及がない |
| 2:11 | 「通って」の読み（とお／つう）がおかしい |
| 2:44 | ラグラージがまだ登場していないのに「登場です」と断定（実際は交代選択中） |
| 3:25 | 「数的優位を取り戻した」は誤り（両者残りポケモン3体で互角） |
| 4:23 | 1分前に起きた出来事（オオニューラ気絶）をここで再実況（＝上記「見つかったNG」254.1sと同一事象） |
| 5:50 | ラグラージも同じ技（れいとうパンチ）を使った可能性があるが無実況 |

**2:44と4:23の2件は、既知のNG系統（switch/move_usedのタイミングずれ・faint二重報告）
と一致する可能性が高く、恒久対策の追加調査候補**（明日以降のNG確認で深掘り予定）。

## 今日の変更まとめ（コード）

- コード変更なし（動画制作のみ）
- `renders/2026-08-23_22-15-43/bubbles.jsonl` 新規作成（git管理対象外）

## 次回やること

- **NGの詳細確認はユーザーが明日以降実施予定**（今日は動画制作のみで終了）
- 上記「2:44」「4:23」の指摘が既知のNG系統（switch/move_usedタイミングずれ・
  faint二重報告）の再発かどうか、原因調査は次回のNG確認時に着手するかどうか判断
- YouTube等への投稿（ユーザー作業）
