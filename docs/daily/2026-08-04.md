# 日報 2026-08-04

## 今日やったこと

### ①実況の口調/知識改善（改善ロードマップ④）実機動画での品質確認 ✅完了

`renders/2026-07-03-23-26-22`でパス1→1.5→ネタバレ検査→パス2を通して検証。

- **バグ発見**: manifest.jsonl 13件中2件（`0003_move_used`/`0004_move_single`）が実況文の代わりに
  「了解しました！花圓くれぴとして、ポケモン対戦実況を担当させていただきます♪ **性格・口調の確認:**...」
  という、指示への相槌・自己紹介をそのまま音声合成してしまう事故を発見（WAVまで合成済み）。
  8/3のプロンプト増量（用語集・戦術知識・few-shot例文追加で指示密度+52%）で誘発されたとみられる
- **修正**: `src/api/server.py`の`_build_vision_prompt`【出力ルール】に「了解しました等の相槌・確認・
  自己紹介は絶対に書かない」を明示追加。保険として`src/pipeline.py`の`_GLITCH_CAUSE_KEYWORDS`
  （AIグリッチ差し替え機構）にも今回のパターンを追加し、事後検出の網もかけた
  （テスト+1件・`tests/test_pipeline.py::test_instruction_echo_keywords`）
- ユーザーがWinSCPでEC2へ再デプロイ→同じ動画でパス1を再実行→**manifest.jsonl 14件全部
  正常な実況文になったことを確認**（了解しました事故は再発なし）
- パス1.5（フィラー）はこの動画では無言区間が40秒超のものが無く0件（正常動作）
- ネタバレ検査 exit 0・パス2合成成功・フレーム3枚（30秒/110秒/263秒）を目視、字幕はみ出し・
  絵文字豆腐化・ネタバレなしを確認
- **ユーザー確認**: 「実況は前より良くなっていると思います」→①クローズ
- コミット済み（`ea164a1`「実況内容改善」・EC2デプロイも完了済み）

### ②`play_and_animate_avatar.py`の呼吸スウェイ/感情別リアクション/ランダム仕草の実機再検証 ✅完了

同じ動画でVMC＋アバター録画（`2026-08-04 14-26-47.mp4`）を実施。

- 常時スウェイ（Spine/Chest呼吸っぽい揺れ）・ランダム首かしげ仕草：実機で動作確認OK
- 感情別ポーズ遷移: ログ上は`表情イベント 4件を検出`（`battle_start`のFun + 「炸裂」キーワード3件のFun）
  が計算通りのタイミングで発火。ユーザーからは「ポーズが全く無かった」と報告があったが、
  録画フレームを実際に抽出して比較（65秒=ニュートラル vs 71.3〜71.8秒=Fun発火直後）した結果、
  **`head_tilt_curious`の首傾きは実際に動作していた**と確認（遷移0.45秒→保持0.6秒→戻す0.45秒＝
  計1.5秒と短く、発生回数も4回と少なかったため見た目で気づきにくかっただけと判明）。
  ユーザー判断: 発生頻度が少ないだけなので**ポーズの長さ/振れ幅は現状維持でOK**
- **新規発見（既知の制約として保留）**: `battle_end`の`victory_arms_up`（バンザイポーズ）が
  一度も発火せず。原因は`_detect_battle_result()`（pipeline.py）が「勝負に勝った/負けた」の
  明示テキストしか判定できず、**「降参」による終了は仕様上「判定不能」**として設計されている
  ため（今回の試合はまさに相手の降参で終了）。実況文自体は「相手の降参で試合終了」と
  正しく言えていた（Bedrockが画像から直接判断できたため）が、ポーズ連動側はテキスト判定
  頼りでこの経路を持っていない。ゲーム側の「降参が選ばれました」というテキスト自体に
  どちらが降参したかの情報が無く、単純な文字列判定では直せない
  （`BattleStateTracker`の気絶数から推測する等、追加の手がかりが必要になる）。
  **ユーザー決定: 直さず既知の制約として保留**（降参終了時はバンザイポーズが出ない）

### 動画作成の続き（v2cアバター合成＋サムネイル）✅完了

`renders/2026-07-03-23-26-22`をそのまま素材に、動画を最後まで完成させた。

- パス2をアバター合成込みで再実行（`--avatar-video "2026-08-04 14-26-47.mp4" --avatar-crop
  "300:480:810:230"`）→ `2026-07-03-23-26-22_commentary_biim.mp4`完成。クロマキー綺麗に抜けて
  緑フリンジなし。フレーム目視で戦況パネルとアバター耳が軽く被る箇所を発見したが
  **ユーザー判断で許容**（既知の軽微な重なりとして記録のみ）
- サムネイル自動生成を初めて実行 → 選ばれたのが`battle_end`（試合結果テキストそのまま）で
  **ユーザーから「ネタバレ過ぎて笑ってしまった」**とのfb。仕組みを説明したところ、
  「他AIVTuberのサムネを参考にしたい」「勝敗でなく構築（自分/相手）の特徴を見せたい」
  「AIVTuberであることも情報として入れたい」という刷新の方向性が出た

### サムネイル刷新（改善ロードマップ⑥続き）✅実装・実機確認済み

`scripts/generate_thumbnail.py`を拡張。

- **ネタバレ対策**: `battle_end`を既定の候補から除外（`faint`>`hp_swing`のみに）。
  旧仕様に戻したい場合は`--allow-result-spoiler`
- **構築アイコン**: `states.jsonl`から陣営（`--team player`/`opponent`、ケースバイケースで
  手動指定する運用）の登場ポケモンを収集→`data/pokedb.sqlite`（既存の図鑑DB）で和名から
  PokeAPI図鑑番号を引き、公式スプライトを`data/pokeapi_cache/icons/`に初回だけ取得・
  キャッシュしてキャプション帯の直上に横並び焼き込み
- **アバター顔**: v2c録画（グリーンバック）からクロップ＋クロマキー抜き
  （`render_commentary_video.py`と同じ`chromakey`+`despill`フィルタを再利用）して右上に焼き込み。
  クロップ既定値`400:400:800:250`は本日の実機フレーム比較で顔がちょうど収まると確認済みの値を流用
- **AIVTuber要素**: 左上に「AI実況」バッジ＋「花圓くれぴ」名前を追加
- テスト+25件程度（ロスター収集・図鑑DB参照・アイコン取得キャッシュ・ffmpegコマンド組み立て・
  合成）、WSL pytest 526件全パス
- 実機（このrenderの実データ）で生成確認: バッジ・アバター顔・アイコン4体とも正常に焼き込み。
  **新規発見（許容・保留）**: ロスターに実際は未登場の「ランクルス」が1体混入
  （`states.jsonl`最初期スナップショットのOCRノイズが原因と推定。既知の「交代時の一瞬の
  誤帰属」ノイズと同系統）。**ユーザー決定: 今回は許容、フィルタ強化は保留**
- コミット済み（`7d956b9`「サムネイル作成修正」）

### ローカルLLM移行（改善ロードマップ⑦）第一歩: VRAM予算の再検証＋フォールバック刷新 ✅完了

ユーザーから「実況内容をもっと良くしたい、RAG/LoRAは効くか」→「ローカルLLMを少しずつ導入したい」
という流れで着手。

- **VRAM予算の訂正**: 旧ADR-003/system-architecture.mdは「Phi-3 mini+3Dモデル同時稼働」
  前提（ライブ配信時代の名残）で組まれていたが、ADR-009以降はパス1（解析）とv2c
  （アバター録画）でフェーズが分離済みと判明。実態は「パス1中は約1〜1.5GB使用・
  最大8.5GB空き」「v2c中はVMCの4〜5GBのみ」。ADR-001/003/system-architecture.mdの
  3ドキュメントを実態に合わせて訂正（コミット未実施分含む）
- **候補モデル比較スクリプト新規作成**: `scripts/compare_local_llm_commentary.py`。
  くれぴのキャラ設定＋用語集＋ダブル戦術知識＋few-shot例文のフルプロンプトで複数モデルに
  同じ状況を投げ、Bedrockの実際の実況文と並べて比較できる
- **5モデルを実機比較**（Llama-3.1-Swallow-8B-Instruct・qwen2.5:7b/14b・gemma2:9b・phi3.5）:
  - qwen2.5:14b: VRAM実測9015MiB（空き1.2GB）でパス1中のOCR/YOLOと合わせ10GB超のリスク
    ＋ロシア語/中国語混入のグリッチ発生 → **除外**
  - Swallow-8B: 事実は正確だが**です・ます調**になりくれぴのキャラ性と乖離 → **除外**
  - phi3.5: 自分/相手の帰属を誤る重大ミス（相手を「我が」と言う等） → **除外**
  - qwen2.5:7b: 技の使い手取り違えミス → **除外**
  - gemma2:9b: 最も安定していたが「前ターンの出来事を今起きたことのように話す」ミスが
    プロンプト修正後も再発・軽微なタメ口崩れ（「〜わね」）も残存
  - **結論**: どのモデルもBedrock置き換えレベルの正確性には未到達。「主実況をローカルに」は時期尚早
- **現実的な一歩として実装**: `src/commentary/phi3_client.py`のフォールバックモデルを
  `phi3:mini`→`gemma2:9b`に変更（VRAM5.4GB・OCR/YOLOと合わせても6.4〜6.9GBで3GB以上の余裕）。
  プロンプトをくれぴのフルプロンプトに刷新し、`battle_context`（戦況構造化情報）も新たに
  渡すよう`pipeline.py`の呼び出し2箇所を更新（従来は生OCRテキストのみで精度が低かった）。
  プロンプト共通要素は`src/commentary/kurepi_persona.py`（新規）に切り出し、比較スクリプトと
  フォールバッククライアントで共有（重複排除）
- テスト+34件（比較スクリプト用21件更新＋phi3_client用13件新規）、WSL pytest 560件全パス
- Bedrock優先の本番経路自体は無変更（フォールバック発火時のみ影響。低リスク）

## 今日はここで区切り（2026-08-04終了）

## 次回やること

1. `_collect_roster`の初期スナップショットノイズ対策（優先度低・保留中）
2. `fist_pump_right`遷移の不自然さ改善（優先度低め・継続）
3. MMDモーション/ポーズの作り込み: 3Dモデル外注完成（9月予定）後に再開
4. 声質変更（RVC）: ITAコーパス録音継続（155/324文・残り169文）
5. プロンプトキャッシュ（cache_control）でのコスト最適化（保留中）
6. ローカルLLM「主実況化」は時期尚早と判断・保留（フォールバックのgemma2:9b化のみ実施済み）
6. ローカルLLM移行（中長期）

**⚠️既知の制約（保留・対応しない）**: 降参による試合終了時、`play_and_animate_avatar.py`の
`victory_arms_up`（勝利ポーズ）が発火しない（`_detect_battle_result`がテキストから
「どちらが降参したか」を判定できないため）。

**定期チェック**: 2026-09-10 Bedrock Haiku EOL／2026年9月頃 3Dモデル外注進捗確認
（ヒタキ様から次回連絡=8月中旬予定）／2026年9月末〜10月頃 チャンピオンズ
レギュレーション確認
