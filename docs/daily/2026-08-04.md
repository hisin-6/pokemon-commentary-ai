# 日報 2026-08-04

## 今日やったこと

### ①実況の口調/知識改善（改善ロードマップ④）実機動画での品質確認 ✅完了

`renders/2026-07-03-23-26-22`でパス1→1.5→ネタバレ検査→パス2を通して検証。

- **バグ発見**: manifest.jsonl 13件中2件（`0003_move_used`/`0004_move_single`）が実況文の代わりに
  「了解しました！花圓くれぴとして、ポケモン対戦実況を担当させていただきます♪ **性格・口調の確認:**...」
  という、指示への相槌・自己紹介をそのまま音声合成してしまう事故を発見（WAVまで合成済み）。
  8/3のプロンプト増量（用語集・戦術知識・few-shot例文追加で指示密度+52%）で誘発されたとみられる
- **修正**: `src/api/server.py`の`_build_vision_prompt`【出力ルール】に「了解しました等の相槌・確認・
  自己紹介は絶対に書かない」を明示追加。保険として`src/pipeline.py`の`_GLITCH_CAUSE_KEYWORDS`
  （AIグリッチ差し替え機構）にも今回のパターンを追加し、事後検出の網もかけた
  （テスト+1件・`tests/test_pipeline.py::test_instruction_echo_keywords`）
- ユーザーがWinSCPでEC2へ再デプロイ→同じ動画でパス1を再実行→**manifest.jsonl 14件全部
  正常な実況文になったことを確認**（了解しました事故は再発なし）
- パス1.5（フィラー）はこの動画では無言区間が40秒超のものが無く0件（正常動作）
- ネタバレ検査 exit 0・パス2合成成功・フレーム3枚（30秒/110秒/263秒）を目視、字幕はみ出し・
  絵文字豆腐化・ネタバレなしを確認
- **ユーザー確認**: 「実況は前より良くなっていると思います」→①クローズ
- ⚠️ `src/api/server.py`/`src/pipeline.py`/`tests/test_pipeline.py`/`tests/test_server.py`の
  変更は**まだgit未コミット**（EC2デプロイは完了済み）

### ②`play_and_animate_avatar.py`の呼吸スウェイ/感情別リアクション/ランダム仕草の実機再検証 ✅完了

同じ動画でVMC＋アバター録画（`2026-08-04 14-26-47.mp4`）を実施。

- 常時スウェイ（Spine/Chest呼吸っぽい揺れ）・ランダム首かしげ仕草：実機で動作確認OK
- 感情別ポーズ遷移: ログ上は`表情イベント 4件を検出`（`battle_start`のFun + 「炸裂」キーワード3件のFun）
  が計算通りのタイミングで発火。ユーザーからは「ポーズが全く無かった」と報告があったが、
  録画フレームを実際に抽出して比較（65秒=ニュートラル vs 71.3〜71.8秒=Fun発火直後）した結果、
  **`head_tilt_curious`の首傾きは実際に動作していた**と確認（遷移0.45秒→保持0.6秒→戻す0.45秒＝
  計1.5秒と短く、発生回数も4回と少なかったため見た目で気づきにくかっただけと判明）。
  ユーザー判断: 発生頻度が少ないだけなので**ポーズの長さ/振れ幅は現状維持でOK**
- **新規発見（既知の制約として保留）**: `battle_end`の`victory_arms_up`（バンザイポーズ）が
  一度も発火せず。原因は`_detect_battle_result()`（pipeline.py）が「勝負に勝った/負けた」の
  明示テキストしか判定できず、**「降参」による終了は仕様上「判定不能」**として設計されている
  ため（今回の試合はまさに相手の降参で終了）。実況文自体は「相手の降参で試合終了」と
  正しく言えていた（Bedrockが画像から直接判断できたため）が、ポーズ連動側はテキスト判定
  頼りでこの経路を持っていない。ゲーム側の「降参が選ばれました」というテキスト自体に
  どちらが降参したかの情報が無く、単純な文字列判定では直せない
  （`BattleStateTracker`の気絶数から推測する等、追加の手がかりが必要になる）。
  **ユーザー決定: 直さず既知の制約として保留**（降参終了時はバンザイポーズが出ない）

## 今日はここで区切り（2026-08-04終了）

## 次回やること

1. `src/api/server.py`/`src/pipeline.py`の今日の修正、**git commit未実施**（要ユーザー確認）
2. `fist_pump_right`遷移の不自然さ改善（優先度低め・継続）
3. MMDモーション/ポーズの作り込み: 3Dモデル外注完成（9月予定）後に再開
4. 声質変更（RVC）: ITAコーパス録音継続（155/324文・残り169文）
5. プロンプトキャッシュ（cache_control）でのコスト最適化（保留中）
6. ローカルLLM移行（中長期）

**⚠️既知の制約（保留・対応しない）**: 降参による試合終了時、`play_and_animate_avatar.py`の
`victory_arms_up`（勝利ポーズ）が発火しない（`_detect_battle_result`がテキストから
「どちらが降参したか」を判定できないため）。

**定期チェック**: 2026-09-10 Bedrock Haiku EOL／2026年9月頃 3Dモデル外注進捗確認
（ヒタキ様から次回連絡=8月中旬予定）／2026年9月末〜10月頃 チャンピオンズ
レギュレーション確認
