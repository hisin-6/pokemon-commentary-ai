# 日報 2026-07-13

## 今日やったこと

### 技帰属修正の残り5本実機検証 ✅（→ `9d8df70` 分クローズ）

18-12-45・06-25-46・07-00-19・08-15-22・20-14-17 の5本を実行し書き起こしと照合。
「ドゲザン→ドドゲザン」補正・「キのつきのひかり」解消・「（推定）」ラベルのBedrock送信を全確認。リグレッションゼロ。

### #0 イダイトウHP表示乖離の解決 ✅（`9257fca`）

真因は「HPpx物理限界（バー15px未満は読めない）」×「数値OCR HPの割当がイベント経路限定」の複合。
`assign_hp_from_ocr()` を新設し、定期OCRで読めた数値HPを**位置ゲート方式**で割り当てるようにした。

- 実装は5ラウンドの実機検証を経て確定: command_cyゲート→スティッキー化→**位置ゲート**（y帯: 自分cy≥950/相手cy≤200 ＋ バー中心cx近接でスロット直接決定）と発展。commandトークンのOCR検出がフレーム単位で欠落するため、トークン依存のゲートは実機で成立しなかった
- 誤読対策: 2回一致ヒステリシス（%形式は3回）＋1桁%クォランティン（ターゲット選択カーソルが先頭桁を遮蔽する「72%→2%」誤読対策）＋px保持値の鮮度再スタンプ抑止
- 結果: イダイトウ 7/201★ピンチをT6/T7で維持・オオニューラ98/155が書き起こしと完全一致・全6本回帰パス
- 検証手法: 診断JSONL（30fps全数OCR）のWSLリプレイが決定打だった

### #2 状態異常の漢字形式対応 ✅（`9257fca`）

`_STATUS_RE` に漢字状態語（眠って/凍りつ/麻痺/猛毒/毒等）＋OCR断片ノイズ許容を追加し、`_STATUS_KANJI_MAP` でひらがな正規形に変換。
ペリッパーの「ねむり」漏れ（16-14-39）が解消。実ログ53本の新旧regex比較で誤爆ゼロを確認（教訓: ひらがな状態語側にノイズスキップを入れると「効果は**いまひ**とつだ」を「まひ」と大量誤検出する）。

### テストスイート全パス化 ✅（`dd5b5e9`）

pre-existing 19件（pipeline15=登録ヒステリシス・command_cyゲートへの未追従／classifier4=英語名廃止への未追従）を全て追従修正。
`server.py` のFlask 2.x専用デコレータをFlask 1.x互換の `@app.route` 形式に変更（ローカルvenvでtest_server.pyが収集エラーになっていた）。
**Windows pytest 223 passed / 0 failed** — 完全グリーン。以降、赤が出たら本物のリグレッション。

### #5 遅延起動時の残骸ロスタークリア ✅（`dd5b5e9`）

battle_startのリセット処理を `_reset_battle_state()` に抽出し、遅延起動（battle_start取り逃しフォールバック）でも前試合ロスターをクリアして起動するよう変更。
08-15-22で検証: eviction 2件→0件・繰り出し引き継ぎで情報欠落なし・リグレッションなし。

## 夕方〜夜: 実況動画ファースト方針への転換（ADR-009）🔄

### 方針決定・ドキュメント整備 ✅

**「しばらくライブはやらず、実況動画を作れるようにする」方針に転換**（実況動画の品質を確立してからライブへ）。

- `docs/adr/ADR-009-video-first-commentary.md` 新規作成。ADR-007・`system-architecture.md` のリアルタイム前提記述（3秒以内・遅延目標<15秒）に「動画モードでは適用外」注記
- ライブモード処理ブロック問題（旧・次回#2）は**凍結**（動画モードは全フレーム処理でフレーム喪失が起きないため）
- **合成方式は2パス方式に決定**: パス1=パイプラインがWAV＋マニフェスト（`event_time`=動画内時刻）を出力／パス2=スケジューリング＋ffmpeg合成。合成やり直しはBedrock再課金なし
- **高密度実況方針**（間があると面白くない）: ①各実況を長め ②台本パス=解析後に試合全体ログをテキストのみでBedrockに1回渡しギャップ用フィラー生成（ネタバレ禁止指示必須）③残りはテンプレ/Phi-3
- コスト試算: Haiku 4.5（$1/$5 per MTok）でイベント実況1回≈0.25円・1試合増量込み10〜15円。コストは制約にしない

### v2レイアウト決定 ✅

実フレーム（16-14-39・ガブリアス42%さめはだシーン）合成のモックアップ4案を作成し（`docs/design/frame-mockups/`）、**案A「biim風＋戦況パネル」を基本に決定**。パネルのHP・技ログ・ターン数はBattleStateTrackerから自動生成。枠は合成パスのレンダリング層に閉じているため後からの変更は再レンダリングのみで可能。

### パス1実装 ✅（未commit）

- **新規 `src/output/render_sink.py`**: `RenderSink`（WAV保存＋`manifest.jsonl`追記＋`render_info.json`・スレッドセーフ）
- **`src/pipeline.py`**: `--render-out <dir>` 追加。`_speak_async(commentary, event_type, event_time)` に拡張し、レンダモードでは再生せず保存。**event_timeは呼び出し元スレッドで確定**（合成スレッド内では動画が先に進む）。faint統合フラッシュは検知時刻 `_pending_faint_time` を使用
- テスト8件追加（`tests/test_render_sink.py`）→ **Windows pytest 231 passed / 0 failed**
- **`pytest.ini` 新設**（`testpaths = tests`）: `logs/test_ocr_*.txt` をpytestが誤収集して収集中断する問題の恒久対策

### 実機検証1回目 → 素材ゼロ（原因判明・実装は正常）

`--render-out` 付きで16-14-39を実行したが**VOICEVOX起動忘れ**で音声合成が全滅（Bedrock実況生成は全件成功）。対策として `run()` 終了時にレンダ保存件数サマリー（0件なら⚠警告）を追加。
副次的な気づき: Bedrockが「# 状況」見出し形式で実況を返すケースが1件（`_clean_commentary`貫通・実況品質改善の題材として記録）。

## 次回やること（明日）

1. **【最優先】パス1実機再検証**: VOICEVOXを起動して16-14-39を `--render-out` 付きで再実行 → `manifest.jsonl` の `event_time` を書き起こしと照合
2. **パス2実装**: `scripts/render_commentary_video.py`（かぶり解決スケジューリング＝後ろ倒しで全部言い切る＋ffmpeg合成＋ダッキング）→ v1初号動画を作る
3. パス1〜2がOKになったらcommit（render_sink・pipeline変更・pytest.ini・ADR-009・モックアップ・日報）
4. その後: 台本パス（ギャップフィラー）→ 高密度実況の実現
5. 継続保留: 繰り出し取りこぼし（ROI/conf）・turn_history→LLM送信・音声出力二重化・技帰属の持ち越し・「# 状況」見出し混入対策
