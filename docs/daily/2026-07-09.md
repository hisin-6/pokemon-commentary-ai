# 日報 2026-07-09

## 今日やったこと

### メモリ・ドキュメント総点検 ✅

- Claudeメモリファイル群（`project_champions_v1`・`project_sprint7_progress`等）を実コードと突き合わせて陳腐化チェック。`MSG_X_MAX`の記載ミス（1800→実際は900）、完了済みタスクの「未着手」表記など修正
- `project_sprint7_progress`（707行）を圧縮。2026-03-30〜05-06の初期実装ログを要約に統合（453行に）
- `docs/`配下の設計書・マニュアル（champions-v1-plan・battle-message-detection・system-architecture・pipeline-logic-overview・ocr-finetune-cycle・yolo-training・screen_capture）を同様に見直し・修正


### `src/`配下 全体コードレビュー→11件修正 ✅（commit `4c48b3b`）

pipeline.py本体と capture/pokedb/api/commentary/output の全モジュールをレビュー。主な修正:
- **clock注入の適用漏れ**: `_MIN_BATTLE_DURATION`・`_BATTLE_START_COOLDOWN`・`_FAINT_PENDING_TIMEOUT`・`BattleMessageParser.DEDUP_TTL`が生の`time.time()`のままだった箇所を動画内時間ベースに統一
- 未使用の`confirm_faint_by_name`（両陣営あいまい版）削除・あいまい名前マッチングを`_fuzzy_name_match()`ヘルパーに統合（7箇所重複解消）
- `_SLOT_BAR_CENTERS`のハードコード値を`hpbar_analyzer.slot_bar_centers()`から算出する形に統合（2箇所の座標二重管理を解消）
- `screen_capture.py`の到達不能コード（Sprint 1時代のCLI・約300行）を削除
- `src/api/server.py`: `MAX_CONTENT_LENGTH`設定・`turn`型検証・AWS例外メッセージの外部漏えい防止
- `pokedb/classifier.py`: SQLite接続の永続化・`ClassifyResult`のfrozen化
- `phi3_client.py`: フォールバックタイムアウト60秒→20秒

### バグ3件発見・修正 ✅（同commit・実機6本で最終検証済み）

`c041924`（同名ミラー戦の誤帰属修正）の実機再検証を依頼したところ、対象の修正自体は正しく機能していたが、検証の過程で**別の未修正バグ**を2件発見。連鎖的に計3件を修正した。

1. **`_update_switch_out`パターン1の誤爆**: 「戻」1文字含有という広すぎるゲートが「しろいハーブで元に戻した」等の無関係メッセージに誤反応し、`set_not_on_field`の無条件両側検索で無関係なポケモンを誤ベンチ化していた。ゲートを「戻って」（連用形）限定に修正
2. **`_ON_FIELD_MISS_THRESHOLD`の単位間違い**: 「3ターン不検出で場から降ろす」の「ターン」が内部イベントカウンター（`self.turn`）であり実ゲームターンではなかったため、メッセージが立て込む区間で誤って早期に場から降ろされていた。`self.game_turn`（実ターン数）ベースに修正
3. **HPピクセル追跡を生存証拠として未考慮**: 名前OCRが独立再検出されないポケモン（07-00-19のオオニューラ）が、HPバーは読めているのに②の修正後も結局は閾値超過で降ろされていた。`update_pixel_hp()`でHPpx読み取り成功時にも`last_seen_turn`をリフレッシュするよう追加

### 実機検証（全6本×3ラウンド）✅

1回目（対象2本: 20-14-17・07-00-19）→c041924対象は確認OK、①のバグを発見
2回目（全6本）→①は解消、07-00-19で②の症状を新規発見
3回目（全6本）→②を修正して再検証、07-00-19で③の症状が別形で残存を発見
4回目（07-00-19 + 残り5本）→③を修正して再検証、**全6本で新規リグレッションなし・問題全解消**。副次効果として16-14-39・18-12-45で見られていた軽微な早期ベンチ表示も解消

新規回帰テスト6件追加（`TestOnFieldMissThresholdUsesGameTurn`3件・`TestUpdateSwitchOut`3件）。pytest全体は pre-existing 19失敗（変わらず）+179パスでリグレッションゼロ。

### commit・push完了

全部まとめて`4c48b3b`としてcommit・`dev/champions-v1`にpush済み。

## 残っている軽微な事項（未対応・優先度低）

- `_update_switch_out`パターン1の`set_not_on_field`は依然無条件両側検索のまま（`c041924`策定時に「メッセージにサイド情報がないため温存」と意図的に据え置いた既知の制限。08-15-22の検証で一時的な誤爆→自己修復のケースを確認したが実害なし）
- 07-00-19終盤でイダイトウのHP表示が書き起こし（7/201≈3.5%）とログ（47%px）で乖離。オンフィールド判定とは無関係の既存HP追跡精度の問題の可能性、未調査

## 次回やること

1. 技帰属の見切れ・誤帰属（使用者名見切れ・隣接誤帰属）
2. 状態異常の漢字形式対応（`_STATUS_RE`がひらがなのみ対応）
3. メッセージROI/conf起因の繰り出し取りこぼし
4. ライブモード処理ブロック問題の方針決定
5. 遅延起動時の残骸ロスタークリア
6. turn_history→LLM送信（EC2 `server.py`更新＋WinSCPデプロイ必要）
7. 音声出力二重化／pre-existingテスト19件の追従修正／`PokeClassifier(game_mode="champions")`
8. 【新規】07-00-19終盤のイダイトウHP表示乖離の調査
