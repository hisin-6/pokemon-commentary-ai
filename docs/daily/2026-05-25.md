# 2026-05-25 日報

## やったこと

### 1. アノテーションレビューツール作成（`scripts/annotation_reviewer.py`）
- Flask製ブラウザUIを新規作成（`http://localhost:5001`）
- 機能：画像ズーム表示・ラベル編集・キーボード操作（→確認/Enterで保存/D削除）
- フィルター：自動修正 / 低信頼 / 中信頼 / 全件 + 「未確認のみ」チェックボックス
- `reviewed=True` フラグで確認済みを記録・再起動時に未確認の先頭に自動ジャンプ

### 2. annotations_fixed.json 全件目視確認
- 全1013件を確認（削除フラグ：19件 → 有効：994件）
- `KNOWN_OCR_ERRORS` に以下を追加：
  - `繁張して` → `緊張して`
  - `はんとう` → `はんどう`
  - `こうけき` → `こうげき`
- `fix_annotations.py` に `」→！` の汎用変換を追加

### 3. LMDB再作成
- `scripts/prepare_lmdb_data.py` 新規作成・実行（train:895件 / val:99件）
- `scripts/create_lmdb.py` 新規作成・実行（`data/ocr_lmdb/train` / `data/ocr_lmdb/val`）

### 4. EasyOCR ファインチューニング（成功）
- `venv\Scripts\python.exe scripts\finetune_ocr.py`（5000iter・約2分）
- **`loaded 44/44 layers (shape-matched, FT mode)`** → 全レイヤーロード成功 ✅
- Best accuracy: **96.2%**（前回80.7%から大幅改善）
- `setup_finetune_model.py` で `pokemon_g2` としてEasyOCRに登録

### 5. パイプライン・スクリプト修正
- `src/capture/screen_capture.py` の `init_reader` を `pokemon_g2` 対応に変更（フォールバック付き）
- `scripts/collect_ocr_crops.py` を `init_reader` 使用に変更（ファインチューニング済みモデルでクロップ収集）
- `scripts/fix_annotations.py` のパスをWSL2固定→相対パスに修正（Windows PowerShellから実行可能に）

### 6. 新10本クロップ収集開始
- 対象：`2026-04-12 16-06-11` 〜 `2026-04-12 17-30-42`（未使用46本中の最初の10本）
- 収集後 `fix_annotations.py` 実行・`annotation_reviewer.py` で目視確認中

### 7. OCR精度比較（2026-04-12 16-14-39.mp4）
- 改善：フシギバナ（前回フシギノリナ）、ウェザーボール「ボ」（前回オ）
- 残課題：ペリッパー→ペリツバー、ウェ→ウエ、HP末尾数字欠落

## 残課題
| # | 問題 | 優先度 |
|---|------|-------|
| 1 | OCR残誤読（ペリッパー・小文字ェ・HP末尾欠落） | 中 → データ積み上げで改善 |
| 2 | battle_start未検知（多試合動画3・4試合目） | 高 |

## 次回やること
1. 新10本クロップ収集・目視確認を完了させる
2. LMDB再作成 → ファインチューニング（2回目）
3. 精度比較で改善確認
4. 残り36本も段階的に追加していく
