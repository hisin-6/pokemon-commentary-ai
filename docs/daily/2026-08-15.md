# 日報 2026-08-15

## 今日やったこと

「実況動画制作を進めながら気づいた不具合を都度直す」運用（2026-08-14決定）の第一弾。
新規録画 `2026-08-14_20-46-44`（`--persona neutral` 初実走・四国めたんモデル）の
review_checklist に記入されたNG群をトリアージし、頻出上位4パターンの恒久対策を
1日でまとめて実装した。commit `0f55c61`「実況NG改善」・EC2デプロイまで完了。
WSL pytest は816件→**876件全パス**。

前提の成果: `--persona neutral` の実機確認は成功（実況テキストが中立口調になっている
ことをチェックリスト上で確認）。

### 1. 降参バグ第3ラウンド: フェーズ遷移経路の未対策が判明→修正

昨日（08-14）の降参バグ修正の実機検証をログ（`logs/pipeline_20260814_211101.log`）で
実施したところ、**修正が一切発動していなかった**。battle_endを発行したのは修正済みの
YOLO終了画面経路ではなく、BattlePhaseClassifierの**フェーズ遷移経路**（`_END_KW`に
「降参が選ばれ」が含まれる）で、「降参が選ばれました」テキストを見た瞬間に即発行→
battle_result未検出のまま実況が「ブリジュラスが落ちた、これで自分の手持ちが全滅です」と
気絶による全滅を捏造していた（checklist #22）。

- フェーズ遷移経路にも降参保留を追加（`_is_surrender_text()`新設・勝敗未検出なら
  既存のWIN/LOSE待機ステートに乗せる・待機中の二重発行ガードも追加）
- `_battle_surrendered`フラグ新設。「降参による決着・倒れた描写の捏造禁止」を
  プロンプト注入（`kurepi_persona.battle_surrendered_line()`が正本・server.pyは手動同期。
  勝ち=相手が降参／負け=自分が降参／不明=断定しない）
- manifest.jsonlのcontextに`battle_surrendered`伝播（実機確認用）

### 2. 技の対象（方向）誤認の恒久対策（最頻NG・7件）

原因はタイミングの構造問題: move_singleは技テキスト検出の瞬間に発火するが、対象を
特定できる証拠（HP減少・状態異常メッセージ・まもる成功）は数秒〜20秒後に画面に出る。
証拠が届く前の情報しか持たないLLMが場のポケモンから対象を推測して外していた。
一方で証拠自体はOCR・HPバー解析で全部取れていた（states.jsonlのHP変化が技の間隔と
完璧に対応・「メタグロスはまひして…」「ライチュウは攻撃から身を守った」メッセージも
実ログに存在）。

後付け生成は生成時点で全データが揃っていることを利用し、「技の直後〜次イベントまでの
観測」から対象を逆引きして`move_target_hint`としてプロンプト注入する方式で対策:

- `_panel_state_history`新設（states.jsonlと同内容をインメモリ蓄積）→HP減少
  （≥3pt・数値HPノイズ対策）と状態異常付与を差分検出
- `_protect_history`＋`_update_protect_history()`新設（「Xは…身を守った」メッセージ・
  OCR誤読断片を許容・陣営は名前直前のプレフィックスで判定）
- プロンプトは「観測に厳密に従う・観測に無いポケモンを対象にしない」指示付き。
  観測ゼロなら従来の「断定しない」安全策（施策A）に倒れる設計
- **実データドライラン検証**（states.jsonl+manifest.jsonlに実装メソッドを直接適用）:
  NG7件中、#3でんじほう・#9アイアンヘッド・#16インファイトの3件が正しいヒントで
  確実修正、残りは観測なし→断定回避で捏造抑止。#20はブリジュラスの防御メッセージが
  実ログにあり実走なら修正見込み

### 3. 気絶イベント未実況の恒久対策（4件・長年の見送り事項をクローズ）

ライチュウ・メタグロス・コノヨザル＋checklist未記載のペリッパーの計4件。ログ調査の
結果、**4匹ともトラッカーレベルでは「気絶確認（メッセージ由来）」まで正しく到達**して
おり、欠けていたのは実況化だけ。原因は4系統の複合:

1. faintイベント時に`_track_new_faints`が現在の**全気絶**を「実況済み」登録→実況対象で
   ない未実況の気絶（ライチュウ）が封印され合成の機会が永久に失われた
2. 保留faint（move_used統合待ち・75秒タイムアウト）がturn_startで持ち越され、
   **次ターンのmove_usedに47.4秒遅れで統合**→#14「メタグロスは耐えきれず交代」の
   時刻も内容もズレた実況に化けた（メタグロス）
3. 連続faintで保留が**無条件上書きで消滅**（コノヨザル）
4. battle_end時に保留が**未フラッシュのまま消滅**（ペリッパー）

さらに合成faint（08-10実装）は相手側のみ対象で、自分側には保険が効かなかった。

対策5点: ①合成faintを自分側にも拡張（`_track_new_faints`が陣営付きで返し、陣営ごとに
「自分の/相手の」でディスパッチ・表情連動のfaint_sideも機能）②faintイベント時の
「実況済み」登録をdiff限定化（特定不能時は従来の全登録にフォールバック）③連続faint時は
保留を先にフラッシュ④battle_end時に保留を先にフラッシュ⑤turn_start到達で保留を
即フラッシュ（ターンまたぎ統合の廃止。フラッシュのevent_timeは検知時刻なので動画内の
配置は気絶した瞬間のまま正確）。昨日の実タイムラインの机上トレースで4件全カバーを確認。

既知の限界: `_announced_faints`は名前ベースのため同名ミラー戦では片側の実況で両側が
実況済み扱いになる（同名ミラー根本解決＝フェーズ2候補とセット）。

### 4. 交代実況のタイミングずれ（#11/#14/#18・3件）

原因は2つ:

1. **switchイベントの発火が早すぎる**: faint→switch_select遷移＝交代選択画面の時点で
   発火し、実際の交代先が判明する「ゆけっ! ペリッパー」は7秒後（実ログ21:22:16→21:22:23）。
   情報がないLLMが直前のブリジュラス交代（30秒前）で穴埋めした（#18）
2. **move_usedのevent_hintが発火タイミングと乖離**: move_used=通信終了はターン冒頭
   （このターンの技がまだ出ていない時点）の発火なのに、指示が「今ターンで使われた技と
   その効果を実況する」だったため、前ターンの技ログ・交代を現在形で語る誤実況の温床に
   なっていた（#11・#14）

対策: ①`_sendout_history`新設（`_note_sendout`で全動画分の繰り出し履歴を蓄積・OCR揺れ
15秒デデュープ）②後付け生成時に`_compute_switch_focus()`でイベント窓の繰り出しを逆引きし
`switch_focus`として注入（switchはevent_hint差し替え・move_usedは状況行）③move_usedの
event_hintを「新しいターンの攻防が始まる場面。戦況全体（HP状況・残り数・有利不利）と
注目ポイントを実況。過去の技や交代を実況し直さない」に刷新（個別の技はmove_singleが
担当する現行分業に合わせた）。

### 5. その他

- faint_focusの根拠文言「残りポケモン数の減少から確定」→「蓄積した戦況データから確定」に
  汎用化（メッセージ由来・自分側の気絶にも整合）
- SKILL.md地雷リストに4件追記（各対策の動作確認方法つき）

## 今日やったこと（続き・午後〜夜セッション）

朝方の「実況NG改善4本」に続き、残NG2件の調査・対策、実況の「開始時挨拶・無言」改善、
`2026-08-14_20-52-59`での動画制作（アバター合成・サムネイル）まで一気通貫で実施。
WSL pytestは876件→**897件全パス**まで積み上げ。

### 6. 残NG対応: タイプ相性ヒントの表現曖昧性＋★ピンチ誤読

朝の4対策の際に見つけていた残り2件（`renders/2026-08-14_20-46-44/review_checklist.md`）を調査:

- **タイプ相性誤り**（「メタグロスのはがね技もエスパー技もいまひとつ」の誤答）: 原因は
  `type_hint`計算自体（`type_chart.py`）は正しいのに、`_matchup_lines`が結果をsetでまとめて
  技タイプ名を落としていたため（実際はがね技だけ「いまひとつ」でエスパー技は等倍＝省略）、
  LLMが「メタグロスの技は」という曖昧な表現から両タイプに誤って敷衍していた。技タイプ名を
  明示する形に修正（`pipeline.py`）
- **陣営逆転寄りのNG**（「メタグロスが落とされ数的不利」→実際はまだ生存中）: 「★ピンチ」
  マーカーの意味をプロンプトが一切説明していなかったのが原因と特定。「HPが少ないだけで
  まだ倒れていない」という注釈を追加（`server.py`）
- 「メタグロス未検出」自体は`_battle_start_roster_incomplete`の保留flush機構が既に機能して
  おり実況への実害なしと確認（既知の軽微パターンと同種）

### 7. アバターのバンザイ腕見切れ対策＋ポーズ場面分岐実装

ユーザーfb「バンザイで腕が画面に映っていない・3Dモデルにもっと動きが欲しい」への対応。
`play_and_animate_avatar.py`の表情→ポーズ対応を、発生場面（`_pose_variant_key`）で分岐する
`_POSE_VARIANTS`に刷新: Joy="default"(battle_end)は`victory_arms_up`のまま・"faint"(1匹
倒す度)は軽い`fist_pump_right`に、Sorrow="sentiment"は`thinking_chin`、Fun="battle_start"は
`lean_back_confident`を新規割当。元々実機検証済みだが未使用だった3ポーズが全部稼働＋
腕を大きく振り上げるvictory_arms_upの発火頻度が下がる（クロップ見切れ対策との合わせ技）。
腕見切れの根本原因（`--avatar-crop`がidle姿勢基準の決め打ちだった）はSKILL.mdに手順追記のみ
（実測は次回録画で）。

### 8. 開始時挨拶＋無言埋め頻度の積極化（過去fbの誤解を訂正）

ユーザー要望「開始時挨拶を入れたい・無言を減らしたい」への対応中、**重要な訂正が判明**:
2026-07-30の「あ、あ、が耳につく・フィラーを減らして」というfbは、無言埋めシステムの
生成頻度ではなく「あー」「えっと」的な**言葉遣いの相槌**への指摘だった（相槌対策は別途
「書き出しのバリエーション」指示で対応済み）。この誤解で`_gap_filler_count`/
`_DEFAULT_MIN_GAP_SEC`を40秒/上限3まで絞ってきたのは過剰対応と判明し、20秒/上限5へ
積極的に戻した。加えて`_INTRO_MIN_GAP_SEC`（8秒）を新設し、動画冒頭区間だけは通常より
短い閾値で対象化＋`is_intro`フラグで挨拶必須指示をプロンプト注入する仕組みを新設。

### 9. 実機検証中に発覚した追加修正（persona付け忘れ／挨拶矛盾／トレーナー名ぼかし）

新規録画`2026-08-14 20-52-59.mp4`でパス1〜パス1.5まで進めた時点でユーザーが「めたんの
状態で作られていない」と気付き、`--persona neutral`の付け忘れ（デフォルトkurepiのまま
実行）と判明→パス1・パス1.5をneutral付きでやり直し。SKILL.mdに「persona」節を新設し
「2026-08-15〜当面neutralで運用」を明記、コマンド例にも組み込んだ。

再検証でさらに2件発見・修正:
- **開始時挨拶が弱い**（「それでは対戦を始めていきます」）: neutralペルソナの「名乗り
  禁止」ルールと新設した「挨拶必須」指示が矛盾し、LLMが挨拶自体を自重していた。「この
  挨拶は名乗りには当たらない」と明示して矛盾を解消
- **相手トレーナー名（`g_cirso`）が画面に映る**: 画面表示自体は既存方針で許容継続、
  ただし**実況テキストでは名前をそのまま読み上げず「相手」「お相手」でぼかす**方針を
  ユーザーが決定。vision/scriptプロンプト両方に安全策を追加

### 10. アバター合成実施（四国めたん公式VRMのクロップ再計測＋x/y-shift新設）

`2026-08-14_20-52-59`でVMC録画ワイプ（v2c）を実施。VRMモデルが四国めたん公式（ピンク髪
メイド服）に切り替わっていたのをClaudeが「水色髪では」と誤認識→ユーザーから複数セッション
目の訂正を受け、新規メモリ`domain_shikoku_metan_appearance`を作成（今後同じ間違いを防ぐ）。

- idle姿勢＋`victory_arms_up`両方のbounding boxを実測し、新クロップ`"663:765:631:314"`
  を確定（旧`HairSample_Female.vrm`用の値から一新）
- 本番録画・合成は成功（クロマキー綺麗・表情/ポーズ連動も正常動作。フレーム抽出タイミングの
  偶然でClaudeが一時「動いてない」と誤判定したが、ユーザーの通し視聴で問題なしと確認）
- **サイズ調整**: 既定344px（画面の18%）は小さすぎるとのfbで700px（36%）に拡大したところ
  右下固定位置のままでは戦況パネルの大部分が隠れる問題が発生→`--avatar-x-shift`/
  `--avatar-y-shift`（右下固定位置からの追加オフセット）を新設し解決。**確定値**:
  `--avatar-crop "663:765:631:314" --avatar-width 700 --avatar-x-shift 80
  --avatar-y-shift 250`

### 11. サムネイル仕様刷新・CLI既定値化

ユーザー指示: ①構築アイコン廃止 ②焼き込みテキストを実況の一部ではなく固定タイトル
ロゴ「ポケモンダブルバトル／AI自動実況」に ③アバター顔をもっと大きく・体まで見せる。

- **折り返しバグ発見・修正**: 改行なしで長い英数字混じりテキストを`--big-logo-text`に
  渡すと、句読点ベースの`_wrap_jp`折り返しが"AI"の途中（バトルA/I自動実況）で機械的に
  割れる実例を発見。`_wrap_to_lines`に明示的な`\n`優先ロジックを追加
- `avatar_face_scale`引数を新設（既定0.34→標準運用0.62）。`--avatar-crop`も動画合成と
  同じ体まで含む値を流用し、テキスト帯の直上ギリギリまで大きく表示
- **CLI既定値を確定仕様に変更**（ユーザー「次も作れるようにしておいて」の依頼）:
  `--label`既定`""`・`--team`既定`None`（アイコン無し）・`--persona`既定`neutral`・
  `--big-logo-text`既定`"ポケモンダブルバトル\nAI自動実況"`・`--avatar-crop`既定
  `"663:765:631:314"`・`--avatar-face-scale`既定`0.62`。`--avatar-video`だけ指定すれば
  確定仕様が再現できることを実機確認

### 12. review_checklist生成（次回NG確認用）

`2026-08-14_20-52-59`用に`scripts/generate_review_checklist.py`でチェックリストを生成
（`renders/2026-08-14_20-52-59/review_checklist.md`）。NGの目視確認はユーザーが翌日実施予定。

### 13. RVCコーパス録音完了

改善ロードマップ①声質変更のITAコーパス（recitation 324文）の録音が本日完了
（294→324文）。次アクションはRVC WebUIでの学習（Train）フェーズ（未着手）。

## 今日の変更まとめ（コード）

- `src/pipeline.py`: `_matchup_lines`タイプ名明示
- `src/api/server.py`: ★ピンチ注釈・挨拶矛盾解消（persona別分岐）・トレーナー名ぼかし・
  `_gap_filler_count`20秒/上限5・is_intro挨拶必須指示
- `scripts/generate_gap_commentary.py`: `_DEFAULT_MIN_GAP_SEC`20秒・`_INTRO_MIN_GAP_SEC`新設
- `scripts/play_and_animate_avatar.py`: `_POSE_VARIANTS`／`_pose_variant_key`新設
- `scripts/render_commentary_video.py`: `--avatar-x-shift`/`--avatar-y-shift`新設
- `scripts/generate_thumbnail.py`: `avatar_face_scale`新設・CLI既定値を確定仕様に変更
- テスト: WSL pytest **876件→897件全パス**（+21件）

**未完了（次回以降）**: ①Windows pytest→commit（複数コミット単位になる想定）
②server.py変更を含む分はEC2再デプロイ ③次回新規録画で開始時挨拶・無言短縮・トレーナー名
ぼかしの実機確認 ④次回アバター録画で`--avatar-crop`のバンザイ込み再計測 ⑤RVC WebUIでの
学習（Train）着手 ⑥`2026-08-14_20-52-59`のreview_checklist目視確認（NG改善へ）

## 次回やること

- **RVC学習（Train）着手**: コーパス録音完了を受けて次のステップへ
- **`2026-08-14_20-52-59`のNG確認**: `review_checklist.md`を目視で埋めてNGを洗い出し、
  見つかった箇所から改善（ユーザー翌日対応予定）
- **実機検証（次の新規録画で）**: 今日の一連の対策の確認ポイント
  - manifest.jsonlのcontextに`move_target_hint`／`switch_focus`／`battle_surrendered`が出ること
  - faint実況が気絶ごとに正しい時刻で出ること
  - `fillers.jsonl`の最初のエントリが挨拶になっているか・無言の体感が減ったか
  - 実況でトレーナー名がそのまま読まれず「相手」等でぼかされているか
- アバター録画時に`--avatar-crop`のバンザイ込み再計測（見切れ対策の仕上げ）
- 実況動画制作を進める中で、気づいた不具合を都度対応（運用継続）
- RVC録音、残り30文
