# 日報 2026-07-23

## 今日やったこと

### RVC録音データの確認 ✅

- `data/voices/`に2ページ目までの録音（レコーディング.wav / (2).wav / (3).wav、計約4.3分）が追加されたので内容確認
- 技術面はすべて問題なし: WAV/48kHz/16bit（マニュアル指定の44.1kHz以上を満たす）、ピークレベル-9〜-18dB（クリッピングなし）、1文ずつ読んで間を置く規則的な無音パターンで録音失敗の形跡なし
- ステレオ収録だが左右チャンネルは完全一致（実質モノラル音声がステレオ箱に入っているだけ）。RVCの前処理でモノラル変換すれば実害なし
- 今後も2ページずつ、録音可能な日に継続する予定

### 実況動画量産: 06-25-46完成 🎬（横展開2本目）

- `renders/06-25-46/06-25-46_commentary_biim.mp4`完成（462.3秒）
- パス1（解析）・パス1.5（フィラー生成）はユーザーがWindows側で実行、③ネタバレ検査・④パス2合成・⑤仕上げ確認はClaude（WSL側）が担当、という役割分担で進行
- ネタバレ検査: 技名先読みなし（exit 0）。気絶・体数の先取りも`states.jsonl`と突き合わせて目視確認し、問題なしと判断
- 7/22にEC2デプロイ済みのserver.py改修（ネタバレ抑制構造強化・turn_history送信等）が実際に機能していることを実データで確認
- **新しい地雷を発見**: フィラー文中の絵文字（💦等のUnicode絵文字ブロック）がMeiryoフォントに無く、字幕が豆腐（□）化する。♪♡等の記号（Miscellaneous Symbolsブロック）は問題なし。fillers.jsonlから該当絵文字を削除→パス2再実行で解消（WAVは元々発声していないので音声とズレない）。`.claude/skills/video-commentary/SKILL.md`の地雷リストに追記済み

### VOICEVOXデフォルト話者を四国めたんに変更 ✅

- 声質変更（RVC方式・ベース話者=四国めたんに決定済み）に合わせ、パイプラインのデフォルト話者をずんだもん（speaker=1）→四国めたん（speaker=2）に変更
- 変更箇所: `src/output/voicevox_client.py`（DEFAULT_SPEAKER）・`src/pipeline.py`（`--speaker`引数デフォルト）・`scripts/generate_gap_commentary.py`（render_info.json欠損時のフォールバック値）
- 話者IDはVOICEVOXの`/speakers`実データで確認済み（四国めたん・ノーマル=2）
- 06-25-46は変更前にパス1実行済みのためずんだもんのまま。**次の07-00-19からめたんの声になる**

### 不具合発見: 控えのポケモンを場にいるかのように実況するケース

- ユーザーから06-25-46の視聴フィードバック: 「場に出ていない（控えにいる）のに居るかのような実況がある」
- 該当箇所を`manifest.jsonl`のcontextと突き合わせて特定: t=339.1sで「フラエッテも100%だから」と発言しているが、渡していたコンテキストでは`控え: ドドゲザン(ひんし) / フラエッテ`と明記されており、データ自体は正しくBedrockに渡っていた
- 原因: `_build_vision_prompt`（`src/api/server.py`）は「場」「控え」の区別自体は説明していたが、**「控えのポケモンに言及する際は控えだと明示すること」という出力ルールが存在しなかった**
- 修正: 出力ルールに以下を追記
  「※ 「控え」のポケモンに言及する場合は必ず「控えの」等を付けて控えだと分かる言い方にすること。「場」にいるかのような表現（現在戦っている前提の言い回し）をしてはいけない。」
- `tests/test_server.py`（63件）はWSL側で全パス確認済み
- **⚠️未デプロイ**: EC2側のserver.pyを更新しないと反映されない。次の動画（07-00-19）を作る前にWinSCP転送→`sudo systemctl restart pokemon-api`が必要

## 次回やること

1. **【要対応】server.py（控え言及ルール追加分）をEC2へデプロイ**（WinSCP転送→`sudo systemctl restart pokemon-api`）。これをやらないと同じ問題が次の動画でも再発する
2. 実況動画残り3本（07-00-19・08-15-22・20-14-17）を同じ役割分担で量産。次回以降は四国めたんの声で生成される
3. RVC: ITAコーパスの録音を継続（2ページずつ）
4. 3Dモデル外注: 本命候補の絵師様のラフデザイン待ち（9月頃に進捗確認を促すこと）
