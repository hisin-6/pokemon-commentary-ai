# 日報 2026-04-20

## 作業内容

### 1. Champions 動画 OCR 確認（`test_ocr_areas.py`）
- 動画: `D:\ゲーム録画\2026-04-14 20-49-38.mp4`
- Champions は `繰り出した`（漢字）形式で OCR が出力されることを確認
- HP OCR: `1`→`7` 誤読（`100%`→`700%`、`187/201`→`787/201`）が多発 → HP は後回し
- ポケモン名誤読: `オオーユーラ`（ニ→ー）、`ブテラ`（プ→ブ）が多発

### 2. switch-in regex 修正（`pipeline.py`）
- `_OPPONENT_SWITCH_IN_RE` に漢字形式 `繰[りゆ]出した` を追加
  - 従来: `く[りゆ]だした`（ひらがな）のみ
  - 追加: `繰[りゆ]出した`（漢字・`り`→`ゆ` OCR誤読バリアント含む）

### 3. PokeClassifier OCR 正規化追加（`src/pokedb/classifier.py`）
- `_best_match` に OCR バリアント試行を追加
  - 大カナ→小カナ変換（`ヤユヨワツ` → `ャュョヮッ`）: `オオーユーラ`→`オオニューラ` ✅
  - 濁点除去（`ブ`→`プ` 等）: `ブテラ`→`プテラ` 100% ✅
- **注意**: `ア行（アイウエオ）` は変換しない（独立した母音のため）

### 4. switch_in PokeClassifier 正規化追加（`pipeline.py`）
- `switch_in` イベント処理で `mark_on_field_by_name` 呼び出し前に PokeClassifier で正規化
  - 従来: OCR生テキスト（`オオニユーラ`）でスロット検索 → 不一致でスキップ
  - 修正後: 正規化（`オオニューラ`）してからスロット検索 → T7 `情報収集中` 解消 ✅

### 5. `mark_on_field_by_name` 修正（`pipeline.py`）
- プレイヤー側スロットのみ検索するように変更
  - 従来: `_find_slot`（両チーム検索）→ 相手に同名ポケモンがいると相手スロットを誤って更新
  - 修正後: `self._player` のみ検索 + 未登録なら新規作成
- 同名ポケモンの誤登録防止フィルター修正（line 997-999）
  - 相手にのみ登録済みの場合のみスキップ（自分側にも登録済みなら通す）

## 確認結果（`pipeline_20260420_224836.log`）
| ターン | 前回 | 今回 |
|--------|------|------|
| T7 | `情報収集中` | `オオニューラ HP:44%` ✅ |
| T8 | `情報収集中` | `オオニューラ / ドドゲザン` ✅ |
| T9〜T13 | `情報収集中` | 正しく表示 ✅ |
| T14〜T16 | `情報収集中` | まだ残存 🔧 |

## 残課題
- T14〜T16 `情報収集中`: `mark_on_field_by_name` 修正を未検証（明日確認）
- T6 `情報収集中`: フラエッテ faint 後 switch-in 未検出（構造的な問題）
- HP OCR `1`→`7` 誤読: 追加学習が必要（後回し）
