# 2026-05-27 日報

## 作業内容

### annotation_reviewer.py バグ修正 ✅
- **バグ**: `markReviewed()`（→キー・次へボタン）が `input.value` ではなく `item.ocr_text`（ロード時の古い値）を送信
- **影響**: テキストを変更した後に Enter 以外（→キー）で次へ進むと変更が消えていた
- **修正**: `markReviewed()` でも `input.value` を使うよう修正

### fix_annotations.py バグ修正 ✅
- **バグ**: 再実行時に `reviewed`/`deleted` フラグのみ引き継ぎ・`ocr_text` は引き継がなかった
- **影響**: 新動画追加後に `fix_annotations.py` 再実行すると、レビューで直した `ocr_text` が全部上書きされた
- **修正**: `reviewed=True` のエントリは `ocr_text` も引き継ぐよう修正

### v3/v4 退行の根本原因特定
- 上記2バグの複合効果が原因（保存バグ + 再実行上書き）
- `chars_differ_only_by_ocr_error` による直接的な誤変換は確認されず（ドライラン0件）

### fix_annotations.py を廃止 ✅
- 自動修正による意図しない変換リスクを排除するため、今後使わない方針に
- **変更内容**:
  - `annotation_reviewer.py`: 読み書き先 `annotations_fixed.json` → `annotations.json`
  - `prepare_lmdb_data.py`: 読み込み先 `annotations_fixed.json` → `annotations.json`
  - `docs/manual/ocr-finetune-cycle.md`: Step 2（fix_annotations）削除・ステップ番号修正
  - `.claude/skills/ocr-finetune/SKILL.md`: 同様に更新

### 新サイクル（6ステップ）
```
収集（--append）→ 目視確認（reviewer）→ LMDB → 学習 → 登録 → 検証
```

### annotations.json 全件 reviewed リセット ✅
- 保存バグで「直したつもりで直っていない」アノテーションがあるため全件やり直し
- `reviewed` フラグ 4777件を削除（`deleted` フラグ 122件は維持）
- 次のステップ: `annotation_reviewer.py` で全件見直し後 → LMDB → finetune（v1ベース・7000iter）

### アノテーション目視確認（開始）✅
- `annotation_reviewer.py`（http://localhost:5001）で確認作業開始
- Enter キー = 保存して次へ の動作確認（テキスト入力中でも有効）
- 本日分: 約1477件完了・残り約3300件

## 現状
- `annotations.json`: 4777件・約1477件レビュー済み・残り約3300件
- 次回: `annotation_reviewer.py` の「未確認のみ」フィルターで続きから再開

## 次回やること
1. `annotation_reviewer.py` で残り約3300件の目視確認（「未確認のみ」フィルター）
2. LMDB再作成 → finetune（v1ベース・7000iter）→ バックアップ `pretrained\pokemon_g2_v5.pth`
3. `test_ocr_areas.py` で精度検証
4. **battle_start未検知の原因深掘り**（多試合動画3・4試合目）
