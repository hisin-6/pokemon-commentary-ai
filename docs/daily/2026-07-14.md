# 日報 2026-07-14

## 今日やったこと

### パス1 実機再検証 ✅（VOICEVOX起動済みで再実行）

16-14-39 を `--render-out` 付きで再実行（`pipeline_20260714_070630.log`）。素材7件（WAV＋manifest.jsonl）が正常保存。

- **event_time の照合: 全7件OK**。診断JSONL（`ocr_diag_20260706_071720.jsonl`・30fps全数OCR）の画面メッセージ出現時刻と比較し、battle_end 383.2s ↔「勝負に負けた」383.1s のドンピシャ一致をはじめ全イベントが整合。faintはメッセージより約3秒先行（faint演出開始を検知・正常）、battle_startは開幕演出後の約9秒遅れ（許容）
- **発見: faint(133.2s)とswitch(144.0s)でBedrockが完全同一の実況文を返し二重保存**（faint保留の早期フラッシュと直後のswitchで文脈がほぼ同一のため）。そのまま合成すると同じセリフが11秒差で2回再生される → パス2のデデュープで解決

### パス2 実装 ✅（`scripts/render_commentary_video.py` 新規）

manifest.jsonl → 実況入りmp4 の合成スクリプト。合成やり直しにBedrock/VOICEVOX再実行は不要（ADR-009の狙い通り）。

処理: event_timeソート → 近接同一文デデュープ（90秒窓） → 後ろ倒しスケジューリング（かぶりは前の実況終了+0.5sまで遅延・遅延20秒超のみ破棄） → 実況トラックWAV生成（スケジュール時刻にサンプル精度で配置・動画と同尺までパディング） → ffmpegでサイドチェインダッキング合成（映像は再エンコなしコピー）。`--dry-run` でスケジュール確認のみも可能。`schedule.json` に採用/破棄の記録を保存。

- render_info.json のWindowsパス（`D:\...`）をWSLの `/mnt/d/...` に自動解決（WSL/Windowsどちらでも実行可能）
- **ffmpeg 4.2互換対応**: WSL標準のffmpeg 4.2はamixの `normalize=0`（4.4以降）が使えない → amix既定の1/2スケーリングを `volume=2` で補償＋実況トラックを動画同尺までパディングして補償を全編一定に
- テスト: `tests/test_render_video.py` 16件新設（デデュープ・スケジューリング・パス解決・トラック生成）。WSLスタンドアロン検証で全PASS（**Windowsフルスイートは未実行**）

### v1初号実況動画 完成 🎬

`renders/16-14-39/16-14-39_commentary.mp4`（6:37・実況6件・デデュープ1件・遅延超過破棄0件）

- 検証: 実況区間のみ音声レベル-25dB前後・区間外は無音＝スケジュール時刻通りに配置。動画長も元と一致（397.57s↔397.59s）
- **発見: 元動画（OBS録画）の音声トラックがほぼ無音**（全区間-91dB・AAC 2kb/s）。ゲーム音が録音されていない。この動画ではダッキングは実質出番なし（実装自体は音入り動画で機能する）。**今後の録画はOBSの音声設定を確認すること**

### 環境: WSLにffmpegをインストール ✅（ユーザー実施・apt・4.2.7）

## 午後: 台本パス（ギャップフィラー）実装 ✅（初号動画の視聴フィードバック対応）

ユーザーの視聴フィードバック: 実況内容のズレは「AIの限界ネタ」として許容。**問題はターン間の無言**（実況は動画の18%のみ・最悪133秒無言）→ ADR-009で構想済みの台本パスを実装。

### 構成（パス1.5 = パス1とパス2の間）

1. **EC2 `/api/script` 新設**（`src/api/server.py`）: 解析済み実況タイムライン＋無言区間リストをテキストのみ1回でBedrockへ→フィラー実況をJSON配列で受け取る。**ネタバレ禁止指示**（各フィラーはその時刻以前の情報のみ・勝敗の先取り禁止・試合開始前の区間は汎用トーク限定）。コードフェンス混入耐性のパーサー付き
2. **`scripts/generate_gap_commentary.py` 新規**: ギャップ検出（既定20秒超・前後2秒マージン・冒頭/末尾対応）→ `/api/script` → time範囲クランプ → VOICEVOX WAV化 → `fillers.jsonl` 出力。`--dry-run`（Bedrock生成の確認まで・VOICEVOX不要）あり。フィラーの再生成はこのスクリプト再実行だけ（数円）
3. **パス2にフィラーマージ**（`render_commentary_video.py`）: `fillers.jsonl` があれば自動マージ。**2段階スケジューリング**＝イベント実況を先に固定配置→フィラーは空き区間に収まる分だけ採用（`fit_fillers`・ずれ12秒超は破棄・イベント実況は絶対に動かさない）
4. **パス1のmanifestに戦況コンテキスト記録**（`pipeline.py`・`render_sink.py`）: `_render_context()`新設。ターン数・両陣営の場/控え・技ログをmanifestエントリの`context`に保存→フィラーの品質向上に使う（レンダーモード以外はNone・既存動作に影響なし）

### 検証

- 新テスト: `tests/test_gap_commentary.py` 8件・`test_render_video.py` +7件・`test_server.py` +14件（台本パス分 計29件追加）
- WSLスタンドアロン検証 19件全PASS＋模擬fillers.jsonlでのパス2 E2Eドライラン（フィラー5件がイベント間に正しく織り込まれ・イベント時刻は不変）
- 実データのギャップ検出: 5箇所・計286秒（動画の72%が無言だった）

## 夕方〜夜: 台本パス実機実行 → フィラー入り動画 v1.1 完成 🎬

### トラブル2件と対処

1. **パス1再実行でmanifest破損（RenderSinkの追記バグ）**: RenderSinkが既存manifestに追記する作りで、再実行時に新旧12件混在＋連番WAV同名衝突（`0004_move_used.wav`の旧音声消失）。→ `RenderSink._clear_previous_run()`実装（再実行時にmanifest/wav/fillersをクリア・テスト2件追加）＋manifestは手動修復（旧runのbattle_start＋新run5件=6件・全WAV実体確認済み）
2. **`/api/script`が404**: EC2側が旧server.py（転送漏れor gunicorn未再起動）→ 再転送＋`sudo systemctl restart pokemon-api`で解消

### フィラー生成と品質チェック

- Bedrock生成6件（tokens_in=1964/out=477≒0.5円・4.4秒）。試合開始前の汎用トークルールも機能
- **ネタバレ1件検出: t=100s「イダイトウが倒されてしまいました」**（気絶は133s・33秒早い死亡宣告）。対処: ①今回分は手動でt=160（気絶後のギャップ）へ移動 ②プロンプトを構造改善=イベントとギャップを時系列に交互に並べ「★区間より下はまだ起きていない」を明示（**server.py再デプロイで有効化・次回**）

### v1.1 完成

`renders/16-14-39/16-14-39_commentary.mp4`（イベント実況6＋フィラー6=12件）
- **発話カバレッジ 18%→28%・最長無言 133s→57s**。音声レベル検証でフィラー3点＋無言区間の配置OK
- 残る57sギャップはT1アニメ中（76〜133s）＝ネタバレフィラーを移動して空いた区間。プロンプト改善後の再生成で埋まる見込み
- `renders/` はユーザーが.gitignoreへ追加済み（mp4が数GBのため）

## 昼〜: v1.2完成＋「ライブ実況化」実装 ✅（未commit）

### v1.2（改善版プロンプトの実証）

再デプロイ＋フィラー再生成→**ネタバレゼロ**（前回アウトのt=100sが汎用実況に・76〜131s区間も充填）。ただし新フィラーは短め（6〜9秒）でカバレッジ22%/最長無言63sと数値は後退→話量アップが次の課題に。

### ライブ実況化（ユーザー要望「試合の流れ全部を渡してリアルタイム実況風に」）

1. **瞬間ログ `timeline.jsonl` 新設**: パイプラインが技を検出した瞬間の動画内時刻を記録（`RenderSink.add_moment`・技ログ登録箇所にフック・再実行時クリア対象）。「t=200秒にドラゴンクローが画面に映った」という時刻アンカーが台本パスで使えるように
2. **プロンプトv2**（`_build_script_prompt`）: ①ライブ実況ペルソナ（録画感の表現を禁止）②📺瞬間ログをタイムラインに時系列混ぜ込み（直後にフィラーを置くと臨場感）③**区間長に応じた件数指定**（約25秒に1件・1〜4件=`_gap_filler_count`）④文字数60〜100字に増量・同一区間内20秒以上離す指示。max_tokens 2000→3000
3. `generate_gap_commentary.py`が`timeline.jsonl`を読んで`moments`として送信（無くても動く=旧素材と後方互換）

テスト+8件（server6・gap_commentary2・render_sink2 ※瞬間ログ分）。WSL検証7件全PASS。

## 午後: v2a（biim風枠＋実況字幕帯）実装 ✅（未commit）

パス2に `--layout biim` を追加（既定は従来の `plain`・枠はレンダリング層に閉じる=ADR-009通り）。

- **レイアウト**: mockup_A_biim.png準拠。ゲーム画面1440×810を左上に配置（`pad`）＋シアン枠線＋右サイドパネル下地（◆戦況/◆技ログの見出しのみ・中身はv2b）＋下部実況帯
- **実況字幕**: schedule.jsonから**ASS字幕**を生成（`build_ass`）して焼き込み。音声とシンクロ表示＋余韻1.5秒（次の実況開始でカット）。イベント=白/フィラー=淡色の2スタイル。フォントはWindows側meiryo参照（`fontsdir=/mnt/c/Windows/Fonts`・WSLに日本語フォント不要）
- レイアウト合成のため映像は再エンコード（libx264 veryfast crf20）。plainは従来通り`-c:v copy`
- 検証: 合成WAV＋実動画でフルレンダリング→フレーム目視でモックアップ一致確認。テスト7件追加（ASS 4・biimコマンド1・※pytest側はTestBuildAss/TestBiimCommand）・WSLスタンドアロン5件PASS
- v2b（戦況パネルの中身）はturn_history時系列エクスポートが前提（バックログ#3と統合設計）・v2c=アバター

## 夕方: ライブ実況化の実機確認 → v2a初号動画（v2.0）完成 🎬（未commit）

### タイムアウト504→修正→フィラー生成成功

- `BEDROCK_TIMEOUT_SEC=5`（vision用）を台本パスが超過→`bedrock_script`クライアント分離（read_timeout=60・vision不変）→再デプロイで解消
- **瞬間ログ15件が機能**: 「インファイト、ねむりごな。相手のフシギバナが…」（📺186.4sの直後190s）のような技直後のライブ風反応を確認。フィラー11件×約13秒と密度も大幅増

### 発見: 区間内の先読みネタバレ（新パターン）→構造対策実装

ギャップ内に📺がある場合、区間頭のフィラーが同区間後方の📺を先読みする（t=85が105〜123sの技を列挙・t=254.7が275.5sのワイドガードを予告）。プロンプト指示だけでは不十分と実証されたため、**`split_gaps_by_moments`**（ギャップを📺時刻で分割・12秒未満の断片は破棄）をクライアント側に実装＝タイムライン上でサブ区間が必ず📺より下に並ぶ構造的対策。**EC2再デプロイ不要**・次回の生成から有効。今回分は手動修正（移動3件・削除2件=事実誤り「イダイトウへの交代」と重複）

### v2a字幕の日本語折り返し問題→修正

ASS/libassの自動折り返しはスペース基準で日本語に効かず帯からはみ出し（実機フレームで発見）→`_wrap_jp`実装（35字で\N挿入・句読点優先ブレーク・閉じ括弧の行頭禁則ぶら下げ）＋フォント40→36

### 成果: `renders/16-14-39/16-14-39_commentary_biim.mp4`

- イベント8＋フィラー7=15件・**発話カバレッジ40%**（初号18%→28%→22%→40%）・最長無言59s
- 収まらないフィラー2件はfit_fillersが正しく自動破棄（安全装置の実機動作確認）
- 既知の残: イベント実況側の「イダイトウへの交代」（310.5s・vision Bedrockの誤認＝AIの限界ネタ枠）

## 夜: 視聴フィードバック対応（字幕大型化・密度アップ・テスト修正）✅（未commit）

1. **pytest失敗1件の修正**: `_update_move_log`の瞬間ログフックが`self._render_sink`直参照で、部分構築Pipelineを使うテストがAttributeError→`getattr(self, "_render_sink", None)`に（実行297件中296パスの1件・修正後は297全パス想定）
2. **字幕の大型化**（スマホ視聴対応）: フォント36→**44の太字**・折り返し31字・帯を12px拡張（高さ230px）で最大4行。実機フレームで帯いっぱいの表示を確認
3. **実況密度アップ＋技直後反応の強化**（server.py・**要EC2再デプロイ**）: `_gap_filler_count`を25秒/件→**18秒/件・上限5件**に増量。プロンプトに「★区間の開始が📺とほぼ同じなら最初のフィラーはその技への反応から始める」を明示（例文付き）。max_tokens 3000→4000

## 夜2: 完全体v2a（v2.1）完成 🎬 — 分割ギャップ＋密度増＋技反応の初生成

- フィラー12件生成（技直後反応が機能: 📺105ねこだまし→115s「オオニューラのねこだまし！」等）→配置8件＋イベント8件=16件
- **発話カバレッジ49%・最長無言40s**（推移: 18→28→22→40→49%）
- **fit_fillersが先読みネタバレ2件を自動破棄**（118.5sソーラービーム4.9秒先読み・330sふいうち13秒先読み→どちらもイベント実況と物理衝突で要ずれ12s超→破棄）。スケジューラの安全装置がネタバレフィルタとして機能。手動修正ゼロで成立
- 破棄4件のうち2件（58s・250s）は内容問題なしの物理不适合（許容）

## 夜3: 字幕帯のフル幅化 ✅（未commit）

視聴fb「テキストエリアの右半分が未使用」→ 字幕帯をゲーム画面下だけ（幅1440px）から**画面フル幅（1888px）**に拡張。右パネルはゲーム画面と同じ高さ（810px）に揃えて右下のデッドスペースを解消。フォント44→**48px**・折り返し37字・最長102字でも3行に収まる。実機フレーム確認済み。
※Windows pytest 297全パスはこの変更前の実行。字幕関連テストは定数参照のみで影響なしだが、commit前に再実行推奨

## 次回

1. **v2.1（完全体）の視聴チェック** → **Windows pytest（297全パス想定）→ 一式commit**（7/14午後〜夜の全部）
2. 他5本の動画で一気通貫の横展開（パス1→1.5→2 biim）
3. 他5本の動画で一気通貫（パス1→1.5→2 biim）横展開
4. v2b=戦況パネル（turn_history時系列エクスポート・バックログ#3と統合設計）→v2c=アバター
5. 実況品質: vision側の交代先誤認（イダイトウ／実際はドドゲザン）・「# 状況」見出し混入（`_clean_commentary`貫通）
